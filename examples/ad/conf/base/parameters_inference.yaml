spark:
  app_name: recsys_tfb_ad_example-inference

inference:
  # 1,200 個 entity（400 個使用者 × 3 個版位）分 10 桶，每桶都有資料
  entity_buckets: 10
  snap_dates:
    - "2026-01-05"   # 資料的最後一週，不在任何訓練／評估 split 裡
  # 不寫 products：item 清單從資料數時，評分的 item 就是模型 preprocessor 裡那份（A52 禁止另寫）
