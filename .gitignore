__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.coverage
htmlcov/
*.parquet
*.pkl
mlruns/
data/*
!data/.gitkeep
.env
*.log
.venv
.venv/
tests/scenarios/output/
.DS_Store
logs/*
.worktrees/
.untouch/

# Spark/Hive driver-side detritus (Derby fallback metastore + warehouse)
metastore_db/
spark-warehouse/

# graphify generated artifacts — all regenerated by code-only rebuild
# (git hook / `/graphify`). GRAPH_REPORT.md is generated too: untracked so
# rebuild churn can't dirty the tree and block checkout/merge. If absent,
# regenerate locally (graphify in .venv [dev] extra) then read it.
graphify-out/cache/
graphify-out/graph.json
graphify-out/graph.html
graphify-out/GRAPH_REPORT.md

# Claude Code session artifacts
.claude/scheduled_tasks.lock
.superpowers/