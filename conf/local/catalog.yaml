# Local dev overrides - paths relative to project root
feature_table:
  type: ParquetDataset
  filepath: data/feature_table.parquet
  backend: pandas

label_table:
  type: ParquetDataset
  filepath: data/label_table.parquet
  backend: pandas

# Dataset Building Pipeline - Intermediate Outputs
sample_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/sample_keys.parquet
  backend: pandas

train_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/train_keys.parquet
  backend: pandas

train_dev_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/train_dev_keys.parquet
  backend: pandas

val_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/val_keys.parquet
  backend: pandas

train_set:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/train_set.parquet
  backend: pandas

train_dev_set:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/train_dev_set.parquet
  backend: pandas

val_set:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/val_set.parquet
  backend: pandas

calibration_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/calibration_keys.parquet
  backend: pandas

test_keys:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/test_keys.parquet
  backend: pandas

calibration_set:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/calibration_set.parquet
  backend: pandas

test_set:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/test_set.parquet
  backend: pandas

X_train:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/X_train.parquet
  backend: pandas

y_train:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/y_train.parquet
  backend: pandas

X_train_dev:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/X_train_dev.parquet
  backend: pandas

y_train_dev:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/y_train_dev.parquet
  backend: pandas

X_val:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/X_val.parquet
  backend: pandas

y_val:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/y_val.parquet
  backend: pandas

X_calibration:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/X_calibration.parquet
  backend: pandas

y_calibration:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/y_calibration.parquet
  backend: pandas

X_test:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/X_test.parquet
  backend: pandas

y_test:
  type: ParquetDataset
  filepath: data/dataset/${dataset_version}/y_test.parquet
  backend: pandas

model:
  type: ModelAdapterDataset
  filepath: data/models/${model_version}/model.txt

best_params:
  type: JSONDataset
  filepath: data/models/${model_version}/best_params.json

evaluation_results:
  type: JSONDataset
  filepath: data/models/${model_version}/evaluation_results.json

preprocessor:
  type: PickleDataset
  filepath: data/dataset/${dataset_version}/preprocessor.pkl

category_mappings:
  type: JSONDataset
  filepath: data/dataset/${dataset_version}/category_mappings.json
