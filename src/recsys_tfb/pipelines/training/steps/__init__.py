"""Mechanism modules for the training pipeline; one concern per module.

Nothing is re-exported here on purpose: ``nodes.py`` imports each module by
name, so the import line itself says which concern a step comes from.
"""
