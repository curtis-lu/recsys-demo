# 架構 / 完整度審查 — round-1

> 角色 C（完整度與架構審查員）。跨整本手冊審查。
> 審查時點：**只有第 01 章寫成 `.md`，第 02–09 章只有 spec §5 的 outline**（依 plan Progress Tracker：Task 1「進行中」，Task 2–12 未開始）。
> 審查對象：spec（§1 目標/讀者/深度、§5 各章大綱、§11 成功標準）＋ 第 01 章實況（作為深度範本）＋ index 骨架。
> 審查範圍限制：只看整體架構、章節順序、覆蓋度、深度是否達成「終極能力目標＝新手讀完能長期穩定營運 data scheduling + feature store」；不查單點技術對錯、不挑逐句易讀性。
> **重要區分**：第 01 章＝已寫的實況（可評實際深度）；02–09＝尚未寫、只能評 outline（評的是「outline 看起來夠不夠 / 有沒有缺項」，不是評文字）。

---

## 0. 一句話總評（先講結論）

骨架作為**「Spark 查詢優化／調校」手冊**是清楚、順序合理、深度範本（第 01 章）足夠的。**但它撐不起 spec §1/§11 自己定的「終極能力目標」**——「長期穩定營運資料排程與特徵庫」。營運能力裡有一整類「**正確性與可靠度**」課題（冪等/可重跑、回填、時間點正確性/特徵洩漏、資料品質驗證、監控與退化偵測、表的生命週期/版本治理、排程相依與編排）目前**只散在第 08 章一節的條列裡被點名，沒有任何一章把它當主體來教**。第 08 章是「把前面的優化技巧套到三個場景」的 playbook，不是「教營運能力」的章。結論：**優化骨幹 OK，營運骨幹缺一大塊**。

---

## (a) 能力地圖：營運排程 + 特徵庫所需能力 → 目前由哪章支撐

把 spec §1/§11 點名的「長期營運」能力逐項列出，對應到目前骨架（含已寫的 01 + outline 的 02–09）哪裡支撐。標記：✅有專節撐｜🟡只在條列被點到/深度不足｜❌完全無人覆蓋。

| # | 營運能力 | 目前由哪章/節支撐 | 評級 |
|---|---|---|---|
| C1 | **叢集執行心智模型**（partition/shuffle/executor/stage/task） | 01（已寫，深度足） | ✅ |
| C2 | **診斷瓶頸**（先量再調、Spark UI、EXPLAIN、認 skew/spill/小檔） | 02（outline 完整） | ✅ |
| C3 | **SQL 寫法優化**（裁剪、join 策略、skew 處理、爆量 join） | 03（outline 完整） | ✅ |
| C4 | **Spark 設定 / 資源配置**（AQE、少數旋鈕、executor 形狀） | 04（outline 完整，且深化 01 的 executor 取捨） | ✅ |
| C5 | **多租戶 / 資源公平**（dynamic allocation、不擾鄰、排程作業穩定要資源） | 04（outline 有點名「營運/多租戶」一節） | 🟡 outline 有，但只一節，能否到「能長期共存於共用叢集」深度待看 |
| C6 | **儲存效率 / 檔案格式 / partition 設計 / 小檔** | 05（outline 完整） | ✅ |
| C7 | **統計維護**（ANALYZE/CBO，AQE 靠它選 join） | 05（outline 有「定期 ANALYZE 重算」） | 🟡 有點名，但「定期/排程化重算」是營運動作，未見有人把它放進排程 playbook |
| C8 | **schema 演進**（加欄/型別變更不打爛下游讀者） | 05（「營運共用資料表」一節）＋08（場景3條列） | 🟡 兩處都只條列點名，無專節展開「怎麼做、怎麼版本化、怎麼通知下游」 |
| C9 | **可重跑 / 冪等 / 失敗重試**（idempotent write、overwrite 安全、partition 級覆寫） | 08（場景2條列「可重跑/冪等、失敗重試」） | 🟡 **只在 08 一行條列**，無原理、無 SQL 範式（`INSERT OVERWRITE` partition、原子性、`_SUCCESS` marker），達不到「能自己營運」 |
| C10 | **排程相依與編排 / 回填(backfill)** | 08（場景2提「partition 設計」、場景3提「回填與重算成本」） | 🟡 **只條列**，無「上下游相依怎麼接、補一段歷史怎麼安全回填、回填與日常排程怎麼不打架」的方法 |
| C11 | **時間點正確性 / 避免特徵洩漏**（point-in-time correctness / leakage） | 08（場景3條列「時間點正確性（避免特徵洩漏）」） | 🟡 **只條列一句**，但這是特徵庫**最核心、最容易出錯、最難自救**的能力，深度嚴重不足 |
| C12 | **資料品質 / 驗證**（row count / null / 範圍 / 唯一性 / 對帳檢查） | — | ❌ **無人覆蓋**（spec 完成定義把它列為能力，但骨架沒有任何章節點到） |
| C13 | **監控與退化偵測**（跑越來越慢、資料量成長、SLA、輸出體積異常） | 08（場景2條列「用 Spark UI 抓退化、監控資料量成長」） | 🟡 只條列；且 Spark UI 看單次 query ≠ 跨天監控趨勢，方法層級不對等 |
| C14 | **表的版本與生命週期維護**（compaction 排程化、保留期/清理、TTL、歷史分區下線） | 05（compaction 點名）＋08 | 🟡 compaction 有點名，但「排程化維護、保留策略、舊分區清理」沒人系統講 |
| C15 | **特徵庫作為共享資料產品**（消費契約、命名/版本、給多消費者併發讀的儲存設計） | 08（場景3條列「供多訓練/推論併發讀的儲存設計」） | 🟡 條列；無「契約/版本/消費者隔離」的展開 |
| C16 | **引擎選用**（Spark vs Hive/Tez vs Impala，含 metadata refresh 對排程的影響） | 06（outline 完整） | ✅ |
| C17 | **DataFrame API（可測試/可重用邏輯）** | 07（outline 完整） | ✅ |

### 能力地圖結論

- **「查詢優化／調校」這條線（C1–C4, C6, C16, C17）：✅ 覆蓋完整、順序合理、深度範本足。** 這是手冊的強項，骨架對得起「Spark 優化參考手冊」這個書名。
- **「長期營運」這條線（C5, C7–C15）：大面積 🟡/❌。** 共通病徵是：**這些能力全被塞進第 08 章「場景 playbook」的條列裡點名，沒有任何一章以它們為主體、用第 01 章那種「建立心智模型 → 具體數字落地 → 明講取捨」的深度展開。**
- **唯一的 ❌（完全無人覆蓋）：C12 資料品質/驗證。** spec §11 完成定義明確把它列為能力地圖該檢查項，但骨架（含 outline）沒有任何章節點到 row-count/null/範圍/對帳這類驗證。這是真缺陷。
- **最危險的 🟡：C11 時間點正確性/特徵洩漏。** 這是特徵庫的命門（做錯會讓模型「偷看未來」、線下漂亮線上崩盤），目前只有第 08 章場景3一句條列。一個新手照現在的 outline 讀完，**不會**有能力自己避免特徵洩漏。

---

## (b) 章節順序與依賴

### 結論：01→09 的學習曲線**順**，沒有「前面用到後面才教的概念」的硬傷。

逐一檢查 forward-reference（前面章節依賴後面才教的概念）：

- **01 → 04**：01 §1.6 講 executor core/memory/台數取捨，明文說「操作細節 forward 到第 04 章」。這是**刻意的、且交代清楚的** forward pointer（「先有直覺就好，第 04 章詳談」），不是硬傷。✅ 已寫的 01 處理得很乾淨。
- **01 → 03/05**：01 §1.6 提到「廣播出去的小表（第 03 章會講）」「HDFS（§1.1）」「第 05 章從儲存端少讀」——都是**指向後面、但當下不要求讀者已懂**的預告，合理。✅
- **02 依賴 01**：診斷要先有心智模型（什麼是 stage/task/shuffle 才看得懂 UI）。順序正確。✅
- **03/04/05 依賴 01+02**：先會看瓶頸，再學對應的優化手段。正確。✅
- **04 的 AQE**：01 §1.7 已先用一句話介紹「AQE 會自動調整執行計畫」，04 再展開。預告得當。✅
- **06 引擎選用排在 03/04/05 之後**：合理——要先懂 Spark 自己怎麼調，才有基準去比 Hive/Impala。✅
- **07 DataFrame API 排第 7**：合理——SQL-first 讀者先把 SQL 線走完，再談「何時改用 API」。✅
- **08 場景 playbook 排倒數第二、綜合前面**：結構上正確（playbook 本就該在工具都教完後收束）。✅
- **09 速查/glossary 收尾**：標準。✅

### 順序層面的**真問題不是「順序錯」，而是「該獨立成章的營運主題被擠進 08 的場景條列」**

也就是：依賴關係沒問題，但**承載營運能力的容器選錯了**。第 08 章的定位（spec §5）是「場景對應：把前面的槓桿綁到三個使用情境，不重教概念」。可是 C9–C15 這些營運能力**在前面任何一章都沒被教過**——08 想「綁」也沒得綁，只能在條列裡第一次、也是唯一一次點名它們。這造成：

- **08 被迫超載**：它同時要當「優化技巧的場景索引」**和**「營運能力的唯一教學點」，兩個角色衝突。
- **營運能力被降格成 checklist**：冪等、回填、特徵洩漏、schema 演進這些需要心智模型 + 取捨討論的主題，在 08 裡只剩「名詞 + 一句對策」，深度遠低於第 01 章範本。

→ 這是本輪**最重要的結構發現**，補強建議見 (d)。

---

## (c) 各章深度是否足以支撐營運（以第 01 章為基準）

**第 01 章（已寫，實況）的深度範本特徵**（拿來當尺）：
- 每個抽象概念都有**具體數字落地**（3000 萬筆帳務、100/50 task、5 台×20 core×80GB vs 20 台×5 core×20GB、overhead 約一成、4–5 core 起手）。
- **明講取捨**（胖 vs 瘦 executor 各四項代價，誠實列正反）。
- **建立心智模型再給操作**（先 shuffle 為什麼貴、再 executor 該多大）。
- **forward pointer 乾淨**（指向後面章節但不要求當下已懂）。

用這把尺量 outline：

| 章 | outline 深度評估 | 是否達 01 範本 |
|---|---|---|
| 02 診斷 | outline 具體（頁籤該看什麼、症狀→看哪→翻哪章對照表），方向對 | 預期可達 ✅ |
| 03 SQL | outline 很實（每招「原理→before/after→UI 變化→取捨」格式），這格式正好是 01 範本的延伸 | 預期可達 ✅ |
| 04 設定 | outline 點名 executor 資源、unified memory、dynamic allocation、多租戶，**深度野心對齊 01** | 預期可達 ✅（**但見下方 C5 多租戶風險**） |
| 05 儲存 | outline 完整，含「營運共用資料表」深度段 | 主體 ✅；但 schema 演進(C8)/維護排程化(C7/C14) **只條列**，未見展開承諾 |
| 06 引擎 | outline 完整（決策表 + 三引擎診斷 + CDP metadata refresh） | 預期可達 ✅ |
| 07 API | outline 完整（破除迷思 + cache/collect/UDF 取捨） | 預期可達 ✅ |
| **08 場景** | **outline 是營運能力的唯一落點，但全是條列**：場景2「可重跑/冪等、失敗重試、抓退化」、場景3「schema 演進、時間點正確性、回填、併發讀」——**每項都只有名詞，沒有 01 那種「心智模型+數字+取捨」** | **❌ 達不到**——這是深度最不足、又最關鍵的一章 |
| 09 速查 | 表格章，性質不同，不需 01 深度 | N/A |

### 深度結論

- **優化線各章（02–07）outline 的深度野心對齊 01 範本，可信能寫到位。**
- **08 是深度黑洞**：它扛著 C9–C15 全部營運能力，卻只給條列深度。**照目前 outline 寫出來的 08，一個新手讀完不會具備冪等、回填、特徵洩漏防護的營運能力**——只會「知道有這些詞」。
- **C5 多租戶**（04 一節）有 outline 但偏薄：「不擾鄰、穩定要資源」要到「能長期共存於共用叢集」需要 queue/pool、優先級、尖峰退讓等，單節恐不夠，需確認 04 是否撐得起。

---

## (d) 具體補強建議（按三級彙整）

### 真缺陷（必補）

**D1.（容器問題，最重要）把「營運能力」從第 08 章的條列裡抽出來，獨立成一個營運專章（或一組節），用第 01 章的深度教。**
- 問題：C9–C15 全擠在 08 場景條列，無教學主體、深度不足，撐不起 spec §11 終極能力目標。
- 建議形態（二選一，定案問使用者）：
  - **(D1-a) 新增一章**，暫名「營運資料排程與資料產品」（插在現 08 之前，即新第 08、原 08 後移）。內容：可重跑/冪等寫入範式（`INSERT OVERWRITE` partition 的原子性、`_SUCCESS` marker、重試安全）、回填(backfill)策略、排程相依、表生命週期維護（compaction/保留期排程化）、監控與退化偵測（跨天趨勢，非單 query）、schema 演進操作手法。**第 08 場景 playbook 則回歸純粹「索引前面章節」的角色**（場景2/3 指向新營運章 + 03/04/05）。
  - **(D1-b) 不新增章**，但把 04（多租戶/資源）、05（共用表/schema/維護）兩章的「營運段」**從條列升級為 01 範本深度的完整小節**，並在 08 場景2/3 改為「指過去」而非「第一次教」。代價：04/05 變厚，營運能力分散兩章、讀者要自己拼。
- 傾向 **D1-a**：營運是一個有內在邏輯的整體（冪等→回填→相依→監控是一條線），拆散到 04/05 會失去這條線；且 spec §1 自己把「營運資料排程、經營多人共用資料產品」抬到與優化並列的高度，配一個專章名正言順。

**D2. 補「資料品質 / 驗證」能力（C12，目前 ❌ 完全無人覆蓋）。**
- spec §11 完成定義明文要求能力地圖檢查此項，骨架卻無任何落點。
- 建議：併入 D1 的營運章一節「產表前/後的資料驗證」——row count 對帳、null/範圍/唯一性檢查、與上游筆數核對、驗證失敗就不發布（fail-the-pipeline）。給 SQL 範式（`SELECT count(*)`, `count(DISTINCT)`, `WHERE col IS NULL` 對帳），對齊讀者 SQL-first。

**D3. 把「時間點正確性 / 特徵洩漏」（C11）從一句條列升級為特徵庫章的核心小節。**
- 這是特徵庫**做錯代價最高、最難自己發現**的能力（leakage 不會報錯，只會讓模型線下虛高）。
- 建議：在 D1 營運章或 08 場景3，用第 01 章深度展開——什麼是特徵洩漏（用銀行例子：用「未來才知道的還款結果」當「當下的特徵」）、as-of join / 截止時間點怎麼在 SQL 裡正確切（`WHERE event_ts <= snapshot_ts`）、常見踩雷（用了含未來資訊的聚合視窗）、怎麼驗。**這條不補，特徵庫能力就是空的。**

### 可加強（斟酌）

**D4. C5 多租戶深度確認。** 04 的「營運/多租戶」outline 偏薄，要支撐「長期共存於共用叢集」可能需補 YARN queue/pool、尖峰退讓、排程作業的資源申請穩定性。寫 04 時量一下單節夠不夠，不夠就升級或併入 D1 營運章。

**D5. C13 監控方法層級對齊。** 08 條列「用 Spark UI 抓退化」——但 Spark UI 看的是單次 query，跨天的「跑越來越慢/資料量成長」是趨勢監控，方法不同（要落地指標、比基準、設告警閾值）。在 D1 營運章把「單次診斷（02 的 Spark UI）」與「長期趨勢監控」明確分層，別讓讀者以為開 Spark UI 就叫監控。

**D6. C7/C14 維護動作排程化。** `ANALYZE`、compaction 在 05 是「該做」，但「**定期/排程化**做、何時觸發、和產表排程怎麼接」是營運動作。建議在 D1 營運章把它們納入「表的例行維護排程」一節，而非只在 05 當一次性操作講。

**D7. index「如何使用本手冊」補一條營運讀者路徑。** 現 index 的切入點全是 ad-hoc/單次問題導向（「查詢很慢」「想改 SQL」…），沒有「我要營運一條排程/特徵庫，該照什麼順序讀」的路徑。補一條「要營運資料產品 → 01→02→03→04→05→（D1 營運章）→08 場景2/3」的學習路線，呼應終極能力目標。

### 誤讀（不改或微調）

**D8.（不改）01→04 的 forward reference 不是缺陷。** 01 §1.6 把 executor 操作細節 forward 到 04，是 spec §1「先直覺→再操作」分層的刻意設計，且 01 交代得很清楚（「先有直覺就好」）。保持。

**D9.（不改）章數骨架 9 章本身合理。** 「依調優槓桿分層 + 早期診斷章 + 末尾場景章」對齊三本權威書編排，優化線無需重排。本輪的補強是**加營運內容**，不是推翻骨架。

**D10.（微調）08 與 09 的「呼應第 02 章對照表」「config 速查集中」等跨章索引設計良好，保留**；只需在落實 D1 後，把 08 的營運條列改成指向新營運章的連結。

---

## 摘要（回傳給主控）

**最重要的結構缺口**：手冊的「Spark 查詢優化」骨幹（第 01–07 章）清楚、順序合理、深度範本（已寫的第 01 章）扎實，作為「優化手冊」完全達標。**但它撐不起 spec §1/§11 自己立的「終極能力目標——長期營運排程 + 特徵庫」**：所有營運正確性/可靠度能力（冪等可重跑、回填、排程相依、**時間點正確性/特徵洩漏**、**資料品質驗證**、監控退化、schema 演進、表生命週期維護）目前**全被塞進第 08 章「場景 playbook」的條列裡點名，沒有任何一章以它們為教學主體**——其中「資料品質/驗證」**完全無人覆蓋（❌）**，「時間點正確性/特徵洩漏」這個特徵庫命門只有**一句條列**。第 08 章因此角色衝突（既當優化索引、又當營運唯一教學點）且深度遠低於第 01 章範本，新手照現 outline 讀完只會「知道有這些詞」，不具備獨立營運能力。

**最建議的補強（單一最高優先）**：**新增一個營運專章**（暫名「營運資料排程與資料產品」，置於現第 08 場景章之前），用第 01 章的深度（心智模型 + 具體銀行數字 + 明講取捨）教冪等/可重跑寫入範式、回填、排程相依、**資料品質驗證（補 C12 這個唯一的 ❌）**、**時間點正確性/特徵洩漏（升級 C11）**、監控退化、表維護排程化；第 08 章則回歸純「場景索引」角色，條列改為指向該營運章與 03/04/05。次優先：index 補一條「營運讀者」學習路線（D7）。優化線（01–07）骨架與順序不需改動。
