# 第 09 章 讀者視角審稿日誌

> 審稿人設定：銀行資料分析師。會寫 SQL、懂業務資料；**沒**學過分散式系統（不知道 shuffle/executor/partition 底層）、**沒** ML 背景。聽過 dbt/Airflow 名字但不熟細節。前置章 01/03/05/08 假設已懂。
> 規則：邊讀邊即時 append。每條標【類型】＋「我會這樣想 / 我會問什麼」。
> 類型代碼：缺脈絡 / 太抽象 / 術語沒先定義 / 概念圖不自明 / 步驟不可操作 / 鷹架洩漏

---

## 邊讀邊記（逐節）

### 標題＋前提區塊（行 1-9）

- **「資料產品」這個詞，標題就用了但要到 §9.1 才解釋。**【術語沒先定義】
  我看到副標「讓資料產品可信」，第一個反應是「資料產品是什麼？是一張表嗎？還是一個系統？」。要往下讀到 §9.1 才知道指的是「被多人多模型長期取用的表/特徵」。建議標題或前提區第一句就一句話點明（例：「你產出的表，只要有別人長期拿去用，就是一個『資料產品』」）。

- 前提區「exit 0」沒解釋。【術語沒先定義】
  「一支作業可以 exit 0」——我大概猜得到是「程式正常結束」，但這是工程術語，第一次出現沒給白話。對只會 SQL 的我，「exit 0 = 程式沒當掉、回傳成功代碼」要當場點一下。（§9.2 開頭有補一句「exit 0 只代表程式沒當掉」，但那已經是下一節了；第一次出現在前提區就該注。）

- 「冪等 INSERT OVERWRITE」「相依就緒閘門」「sensor／_SUCCESS」當已懂帶過 —— 這合理（08 章前提），不扣分。但 §9.4/§9.5 多次回扣這些，若讀者 08 章記憶模糊會稍微吃力，屬可接受。

- 整體：前提區把「跑成功 ≠ 算對」這個全章主軸講得很清楚，這點很好——我一讀就知道這章要幹嘛。

### §9.1 本章地圖＋四支柱 Mermaid 圖（行 11-33）

- 「資料產品」終於在這裡定義了（「好幾個模型、好幾個同事長期取用的資料產品」），但如上條所說，這定義來得有點晚（標題已用兩次）。【缺脈絡】

- **`cust_feature` 這個表名第一次出現，沒說它是什麼。**【缺脈絡】
  「你產的 `cust_feature`」——我猜是「客戶特徵表」，但它的 grain（一列代表什麼）、有哪些欄，要到 §9.2 的 ⚠️ 框才講「grain 是 (cust_id, snapshot_date)」。這張表是全章貫穿範例，建議在 §9.1 第一次出現時就用一句話交代：「`cust_feature`＝每個客戶在每個月底長什麼樣的特徵表，一列 = 一個客戶 × 一個月」。否則前兩節我都在猜這表的形狀。

- **「扇出（fan-out）」在 §9.1 沒出現、§9.4 才出現並括號註原文** —— 順序 OK，但 §9.1 圖下文字「好幾個模型取用」其實就是 fan-out，可在此先白話鋪一句，§9.4 再給術語。屬可加強。

- **四支柱 Mermaid 圖：大致自明，但有兩個小卡點。**【概念圖不自明】
  1. 四個方框內各塞了「標題＋一句說明」，資訊密度不錯，我能看懂四件事各是什麼。但「§9.2 對昨天暴跌」「§9.3 特徵洩漏」這些詞在圖裡是**第一次出現**（正文還沒講），我看圖時會卡在「特徵洩漏是什麼」。圖通常會被當「先看的地圖」，建議圖只放最白話的概括、術語留到正文首次定義，或接受「圖只是預告、看不懂正文會講」。
  2. 圖是「四件事 → 可信的資料產品」的匯流結構，這個我看得懂，沒問題。

- 「四件互不重疊的事」——說互不重疊，但 §9.4 契約三又說「就緒 = 分區完整且過了 §9.2 品質閘」，§9.5 又接 §9.4 的版本化。讀下來其實四件事彼此勾連頗多。「互不重疊」這個宣稱我讀到後面會覺得有點言過其實。【缺脈絡/輕微主旨張力】

### §9.2 資料品質驗證（行 36-107）

- 開頭「exit 0 只代表程式沒當掉」——這裡補上白話了，很好。但如前述，第一次出現是在前提區，注釋來得晚一節。【術語沒先定義】

- 五個驗證面向（非空/唯一/值域/參照完整/漂移）講得清楚，每個都有銀行化的白話例子（key null、label 跑出 2、外鍵找不到），這段我讀得很順。讚。

- **「grain」這個詞在 ⚠️ 框（行 66）才第一次定義性使用，但行 40 的「唯一（grain 不該重複）」已經先用了。**【術語沒先定義】
  行 40 括號裡「grain 不該重複」——我當下不知道 grain 是什麼（顆粒度？主鍵？）。要到行 66 ⚠️ 框「特徵表 grain 是 (cust_id, snapshot_date)」才從上下文推出來「grain = 一列代表的最小單位 / 該唯一的鍵組合」。建議行 40 首次出現就一句話定義：「grain（一列代表什麼，例如『一個客戶一個月一列』）」。

- **「generic test」是 dbt 行話，沒解釋。**【術語沒先定義】
  「dbt 內建四個 generic test」——我不熟 dbt，「generic test」對我是黑話。我能從上下文猜「就是 dbt 預先做好、可重複套用的檢查」，但沒當場點明。建議第一次出現補半句白話。

- **YAML 範例：對「只會 SQL、不熟 dbt」的我，這段 YAML 的『它跑在哪、怎麼觸發』缺脈絡。**【缺脈絡】
  我看得懂 YAML 在宣告「cust_id 要 not_null、label 只能 0/1」，但我會問：這個 `.yml` 檔放哪？誰去讀它？是我手動跑 `dbt test` 還是排程自動跑？正文行 42 有寫「`dbt test` 一跑就驗」，算有交代，但「宣告式」這個詞（行 42「變成宣告式檢查」）對沒寫過 dbt/不熟『宣告式 vs 命令式』的我略抽象——我會想「宣告式是什麼意思？跟我平常寫 SQL（一步步算）有何不同？」建議一句白話：「你只『宣告』要驗什麼、不用自己寫驗的程式，dbt 照著生 SQL 去跑」。

- **`ref('dim_product')` 的 `ref()` 沒解釋。**【術語沒先定義】
  `relationships: to: ref('dim_product')`——`ref()` 是 dbt 的核心語法（指向另一個 model），但我不熟 dbt，看到 `ref('dim_product')` 會卡「這個 ref 是什麼函式？dim_product 是表還是 model？」。雖然不影響理解大意，但屬第一次出現未定義的術語。

- ⚠️ 框（單欄 unique 會誤判）講得非常好——這正是我這種「會抄範例」的人最容易踩的雷，而且用 (cust_id, snapshot_date) 具體解釋了為什麼單欄 unique 會假警報。這段是全節最有價值的提醒。

- **`dbt_utils.unique_combination_of_columns`、「dbt-utils 套件」「自訂 generic test」** —— 點到即止可接受（正文有說「得用某套件或自訂」），但對不熟 dbt 的我，「套件」要另外裝、「自訂 generic test」怎麼自訂，都是未展開的黑盒。屬可接受的「指路不展開」，但會讓我覺得「那我到底該怎麼做」懸著。【步驟不可操作（輕微）】

- **severity / error_if / warn_if（行 68-83）**：概念講得清楚（error 硬擋、warn 示警、閾值升級）。`error_if: ">1000"` 的例子具體、好懂。這段好。
  - 小卡點：行 83「error 是硬閘門、warn 是退化訊號」——「退化訊號」這詞接的是 08 章「監控與退化」，若我 08 章印象淡，「退化」會有點抽象（退化指什麼退化？資料品質慢慢變差？）。屬輕微缺脈絡。

- **純 Spark SQL 品質閘（行 85-103）**：
  - 這段 SQL 我看得懂（union 各檢查、HAVING > 0 才回列）。很實用。
  - **但「外層腳本拿這段的回傳列數判生死」——『怎麼判』沒給可操作步驟。**【步驟不可操作】
    正文行 103「外層腳本（cron/Airflow）拿這段的回傳列數判生死：有列＝有違規＝讓這一步失敗」。我會問：具體怎麼做？是 `spark-sql` 跑完數 stdout 行數？還是包成 `COUNT(*)` 再 `if`？對只會寫 SQL、不熟 shell 的我，「拿回傳列數讓作業 exit 非 0」這步是空的——我不知道怎麼把「SQL 回了 N 列」變成「shell exit 1」。08 章 §8.5 的 `set -euo pipefail` 有被引用，但那管的是「指令失敗就停」，不直接等於「SQL 回了列就停」。這裡缺一個最小可操作的橋。

- 行 105「警報要有真去處」三步（驗出 → 擋住 → 通知）總結得好，原則清楚。`on_failure_callback` 是 Airflow 術語、點到即止可接受。

### §9.3 時間點正確性與特徵洩漏（行 111-168）— 本章概念最重一節

- **「特徵洩漏 / data leakage」第一次出現有當場白話定義（行 113），這點做得好。**
  「用了預測當下還不可能知道的資訊去當特徵」——這句白話我懂。而且「離線評估虛高、上線原形畢露」的後果講得很生動。讚。
  - 不過：「特徵（feature）」這個詞，整章預設我懂。對「沒 ML 背景」的我，「特徵」到底是什麼？我會猜「拿來餵模型的欄位 / 客戶的屬性」，大致對，但全章從沒給「特徵 = 描述每個客戶的一個數字/屬性，模型用它來預測」這種一句話白話。這是全章一個隱形前提——CLAUDE 設定我「沒 ML 背景」，那「特徵」「label」「模型訓練 / 評估 / 上線推論」這組詞其實都該有個最小錨點。【術語沒先定義／全章隱形前提】

- **「label」這個詞全章大量使用，從沒定義。**【術語沒先定義】
  §9.2 行 53「label 只能是 0 或 1」、§9.3「label 是『6 月有沒有違約』」——我能從例子反推「label = 要預測的答案（買沒買 / 違約沒違約）」，行 117 括號也有「label（要預測的答案）」。算有補，但補得零散、靠讀者自己拼。對沒 ML 背景的我，建議在第一次出現（§9.2）就一句話定錨。

- **snapshot-partition 模型（行 115）：這是本節核心概念，解釋算清楚但有依賴。**【太抽象 → 部分落地】
  「一列一個 (entity, snapshot_date)，每個 snapshot_date 分區＝在那個時間點每個客戶長什麼樣」——這句配上後面的銀行例子（5/31 的特徵），我大致跟得上。但：
  - **「entity」突然冒出來（行 115），前面都講 cust_id / 客戶。**【術語沒先定義】我會卡「entity 跟 cust_id 是同一個嗎？」其實是抽象框架詞（entity＝客戶），但本章其他地方都用 cust_id，這裡突然切成 entity 會讓我懷疑是不是另一個東西。建議統一用 cust_id，或一句話說「entity（這裡就是客戶）」。
  - **「snapshot」這個詞本身**——前提區說 08 章沒教 snapshot（08 前提列的是 partition、external/managed 表等）。snapshot 像是本章/本書環境的核心詞，但我不確定前面章節有沒有定義過。若沒有，「snapshot＝在某個時間點對全體客戶拍的一張快照」這個白話最好在這裡（或更早）給足。讀者問：snapshot 跟 partition 是同一回事嗎？（這裡看起來 snapshot_date 就是分區鍵，但沒明說「一個 snapshot = 一個 snapshot_date 分區」。）

- **時間切線 Mermaid 圖（行 119-124）：大致自明，但有卡點。**【概念圖不自明】
  - 左框「≤ 2026-05-31」「只能用這一側算特徵」、右框「拿來當特徵 = 洩漏 ❌」——這個「左邊能用、右邊不能用」的二分我看得懂，圖傳達的核心訊息清楚。讚。
  - 卡點 1：「label 觀察窗」這詞在圖裡出現（右框「6 月有沒有買（label 觀察窗）」），但「觀察窗 / label 窗」這個概念正文要到行 129 才講（「label 窗」「特徵窗」）。圖先用了、正文後解釋。【概念圖不自明】我看圖時會卡「觀察窗是什麼」。
  - 卡點 2：圖只畫了「特徵側 vs label 側」，但沒畫出「要預測的時間點（prediction point）」在哪。正文行 113 強調「在要做預測的那個時間點」，但圖裡沒有這個點的位置——切線本身就是預測點嗎？我會想確認「切線 = 做預測的當下」這個對應。建議圖上標一下。

- 洩漏三種長相（行 126-130）：例子都很具體、銀行化（帳務全加總、6 月還款混進特徵、join 漏鎖 snapshot）。這段好懂。讚。
  - 小卡點：第 2 類「label 期間混進特徵……特徵窗和 label 窗重疊」——「特徵窗」「label 窗」是這裡第一次明確當術語用，但沒有一個圖或一句話把「窗」這個比喻釘死（窗 = 一段時間區間）。配合上面圖的「label 觀察窗」，建議用一張小時間軸把「特徵窗 ≤ 切線」「label 窗 > 切線」兩段畫出來，「窗」就自明了。

- 正解 SQL（行 132-162）：❌/✓ 對照非常清楚。`WHERE txn_date <= DATE '2026-05-31'` 加時間上界、join 鎖 snapshot_date——這兩段我都看得懂、可照抄。這是全節最實用的部分。讚。

- **training–serving skew（行 164）第一次出現，括號原文但白話偏薄。**【術語沒先定義/太抽象】
  「另一種洩漏的近親（training–serving skew）」——正文有解釋「線上看到的特徵分佈和訓練時一致 / 對不上」，但對沒 ML 背景的我，「特徵分佈」「training / serving」這組詞要靠猜。「serving＝上線推論、training＝訓練」沒有當場對照。建議一句白話：「training（訓練模型時）和 serving（上線、真的拿模型去算每個客戶分數時）兩邊特徵的算法要一樣，不然就是另一種坑」。
  - 「上線推論」「算分數」這些動作，沒 ML 背景的我其實也需要一個最小心智模型：模型先用歷史資料『訓練』出來，之後拿來對新客戶『推論 / 算分數』。全章假設我有這個圖像，但沒給。

- **as-of join「一段帶過」（行 166）—— 對我這個讀者，這段反而是全節最抽象、最讀不下去的地方。**【太抽象/缺脈絡】
  標題自己說「為什麼這裡用不到 as-of join（一段帶過）」，但這「一段」資訊量很大、術語密集：
  - 「as-of join（時間點 join／point-in-time join）」「不等值時間對齊」「帶任意 effective_date 的事件流」「在不規則時間點更新」「用 window function 模擬」——這一串對我幾乎每個都要停下來想。
  - 我會問：既然「用不到」，為什麼要花這麼大一段講它是什麼、又講 Spark 3.3 沒有原生語法、又講要用 window function 模擬？對我這個讀者，「用不到的東西」講這麼細，我會困惑「那我到底要不要管它」。這段更像是寫給「已經聽過 as-of join、會擔心『欸怎麼沒用它』」的進階讀者，不是寫給我。
  - 對照寫作指引 §11「不要替讀者回答他不會問的問題」：我根本還沒想到 as-of join 這層，這段是先發制人地澄清一個我不會問的疑慮。【可能是給作者潔癖、非讀者需要】
  - 若要保留，建議：大幅壓縮成一兩句白話（「有種叫 as-of join 的進階時間對齊技術，但因為我們把特徵固定成每月一張快照，用一個 = 條件對齊就好，完全用不到它，這也正是 snapshot 設計的好處」），把「effective_date 事件流 / window function 模擬 / Spark 無原生語法」這些細節降級或移到 footer。

- 行 166 末「snapshot-partition 是個好設計」的論證（昂貴對齊被預先固化成分區）——邏輯我能跟，但要先扛得住上面那串 as-of 術語才到得了這個結論。對我這讀者，結論（好設計）比論證（為什麼）更該先給。【缺脈絡/順序】

