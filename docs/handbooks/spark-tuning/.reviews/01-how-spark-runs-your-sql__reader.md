# 讀者審稿日誌 — 01 · Spark 怎麼跑你的 SQL

> 審稿人設定：銀行資料分析師，會寫 SQL、熟業務資料，**沒學過分散式系統**，不懂 shuffle / executor / partition 底層。第一次讀這份手冊，沒有任何前置章可依賴。
> 任務：標卡關點 + 類型 + 「我會這樣想 / 想問什麼」。不查技術對錯。

類型標記：缺脈絡 / 太抽象 / 術語沒先定義 / 概念圖不自明 / 步驟不可操作 / 鷹架洩漏

---

## 標題與前提框（L1–L7）

**[整體印象 — 好的]** 開頭框就講清楚前提「你會寫 SQL、不需要分散式背景」，符合寫作指引 §1。這讓我（讀者）一開始就放心：作者知道我不懂底層。心智模型的定位也清楚——這章不教調優、先建模型。這點我很買單。

**[術語沒先定義] L5「心智模型」**
- 我會想：心智模型是中文，不卡。OK。

**[小疑問 — 缺脈絡] L5「最貴的那件事」**
- 開頭就拋「最貴的那件事」，但還沒講是哪件事。讀到 1.5 才知道是 shuffle。對讀者來說這是「先賣關子」，可接受，但我第一次讀到「最貴」會懸著。屬可接受的鋪陳，不算缺陷。

---

## 1.1 你的查詢，其實是一群機器一起做（L9–L36）

**[好的]** 用「自己筆電一顆 CPU 從頭讀到尾」對比，非常貼近我的經驗，立刻懂「Spark 不是這樣」。partition 用「切成很多塊、散在多台機器」+ 信用卡帳務表 3000 萬筆切幾百塊、每塊幾十萬筆——具體數字落地，這段我完全跟得上。Driver/Executor 用「指揮官 / 工人」比喻，好懂。

**[術語沒先定義 — 真缺陷] L33「CDP」「YARN」**
- 原文：「在 CDP 上，這些 executor 要用多少台、多大記憶體，是由 YARN（叢集的資源管家）分配的。」
- 「YARN」當場有括號解釋（資源管家），OK。但「**CDP**」完全沒解釋，憑空冒出。
- 我會這樣想：CDP 是什麼？是我們銀行用的某個平台嗎？是 Spark 的一個版本嗎？我不知道，這個縮寫對我沒有任何意義。我會卡一下，然後猜「大概是某個叢集平台」繼續讀，但心裡留個疙瘩。
- 建議：CDP 第一次出現要當場一句話說明（例如「CDP（我們公司跑 Spark 的那個叢集平台）」）。

**[概念圖不自明 — 可加強] L20–31 第一張 Mermaid 圖**
- 圖本身好懂：SQL → Driver → 三個 Executor，每個處理一部分 partition。箭頭方向清楚。
- 小卡點：圖裡 Driver 指向 Executor 的箭頭（D --> E1 等），我會理解成「Driver 把工作派給 Executor」。但 1.1 開頭說 Driver 還要「把結果收回來」，圖上沒有反向箭頭表示結果回傳。我會想：「結果怎麼回到 Driver 的？」這是小事，但圖只畫了單向，跟文字「收結果」對不太起來。
- 屬可加強，不影響理解主軸。

**[太抽象 → 但有救 L35「主軸」]**
- 「資料怎麼切、要不要在機器之間搬動，就決定了快慢——這是本手冊反覆出現的主軸」——這句很好，把為什麼重要講清楚了，且預告了主軸。讚。

---

## 1.2 Spark 不會馬上算：先攢計畫，再一次跑（L39–L51）

**[好的]** lazy evaluation 用「攢成一份待辦計畫、先不動手」解釋，action 用「真的要結果的事」+ 三個例子（存表/撈回來看/count），這段我懂。中文「延遲求值」也有附。

**[術語沒先定義 — 可加強] L48「collect」、L48「Hue」**
- 原文：「把結果撈回來看（例如 `collect`、或 Hue 裡按下執行去顯示資料）」
- 「`collect`」：我沒寫過 Spark 程式，只會 SQL。`collect` 是程式裡的一個指令嗎？對我（純 SQL 使用者）這個詞沒被解釋。我會猜「大概是某個把資料拉回來的指令」。可接受，因為旁邊有中文「撈回來看」，但嚴格說它是個沒定義的 API 名。
- 「Hue」：也是憑空出現的工具名。我會想：「Hue 是我們查 SQL 的那個網頁介面嗎？」如果是，那其實對我很親切（因為我就是用那個跑 SQL）；但作者沒說 Hue 是什麼，我得自己猜。建議一句話點明（「Hue（你平常打 SQL 的那個網頁工具）」）——這樣反而能拉近距離。

**[好的] L51「為什麼這件事重要」**
- 解釋了 lazy 的好處（Spark 看得到整份計畫才能優化，例如把 WHERE 提早、砍掉用不到的欄位）。我懂為什麼要先攢再跑了。

---

## 1.3 從 SQL 到一群 task：計畫怎麼變成實際工作（L55–L74）

**[概念圖不自明 — 真缺陷] L59–66 第二張 Mermaid 圖（SQL → Logical Plan → Catalyst → Physical Plan → Stage → Task）**
- 圖裡一口氣出現「**Logical Plan**」「**Physical Plan**」兩個全英文術語，圖上只有「要做什麼 / 實際執行步驟」的小字註解，但這兩個詞在**圖出現的當下還沒在正文定義過**。
- 我會這樣想：Logical Plan、Physical Plan 是什麼？「邏輯計畫 / 實體計畫」？我大概從註解猜到「要做什麼 vs 怎麼做」，但這兩個正式英文名詞我是第一次看到，圖直接丟給我。
- 正文（L68 之後）只回頭定義了 Catalyst / Stage / Task 三個詞，**唯獨 Logical Plan 和 Physical Plan 沒有在「先對上號」清單裡**。讀者會發現圖上有 5 個英文方塊，正文只解釋了 3 個。這是明顯的落差。
- 建議：要嘛在「先對上號」清單補上 Logical/Physical Plan 的一句話定義，要嘛圖上就別用英文（用「邏輯計畫」並在旁邊說清楚）。

**[術語沒先定義 — 真缺陷] L64「資料重分配」vs 後面的「shuffle」**
- 圖上 L64：「切成 Stage / 每遇到一次資料重分配切一刀」。這裡用「資料重分配」這個詞，但 1.4 才會把這件事正名為「shuffle」、1.3 正文 L71 又說「需要搬資料就切下一個 stage」。
- 我會想：「資料重分配」「搬資料」「shuffle」是同一件事嗎？三個地方用了三種講法，我不確定它們指的是不是同一個動作。對讀者來說術語不統一會懷疑「是不是有不同的東西我沒分清」。
- 建議：1.3 提到切 stage 的那一刀時，至少預告一句「這個『搬資料』之後會正名為 shuffle」，把三種講法綁成一個。

**[好的] L72 Task 定義**
- 「一個 partition 對應一個 task，100 個 partition 就是 100 個 task」——具體、好記，前後呼應 1.1 的 partition。讚。

**[鷹架洩漏？— 微調] L74「這正是下一節」**
- 原文：「所以『為什麼要切 stage』這個問題，答案就藏在『搬不搬資料』——這正是下一節。」
- 「這正是下一節」是給讀者的導覽，不算純鷹架。但寫作指引 §6 對「先說/這一節」類後設旁白很敏感。這句偏導覽、可接受，屬微調等級（若要更乾淨可改成「下一節就講搬不搬資料」）。

---

## 1.4 兩種運算：自己算的（便宜）vs 要交換的（貴）（L78–L104）

**[好的]** 窄依賴/寬依賴用「能不能自己算完 vs 得跟別塊交換」區分，配上 SQL 例子（WHERE/SELECT/逐列計算 是窄；GROUP BY/JOIN/DISTINCT/ORDER BY 是寬）。這些 SQL 我天天寫，能直接對上自己懂的東西，非常好。

**[術語沒先定義 — 可加強] L82「narrow」、L85「wide」、L88「key」**
- 「narrow」「wide」英文括號緊跟中文，OK。
- 「**key**」（L85「同一個 key」、L86「join key」）：對 SQL 使用者，key 大概能從上下文猜成「join 或 group by 用的那個欄位」。但它沒被當場定義。我會想：「key 就是我 GROUP BY / JOIN ON 的那個欄位吧？」——多半猜對，但嚴格說是沒定義的詞。建議首次出現點一句（「key 就是你 GROUP BY 或 JOIN ON 用的那個欄位」）。

**[概念圖不自明 — 可加強] L90–104 第三張 Mermaid 圖（窄 vs 寬/shuffle）**
- 窄依賴那半邊（partition A → 結果 A，partition B → 結果 B）一看就懂。
- 寬依賴那半邊：partition A、B 都標「客戶 1、2 的交易混在一起」→ shuffle 圓圈 → 拆成「客戶 1 的全部交易 / 客戶 2 的全部交易」。這個其實畫得不錯，能看到「混在一起 → 重新分配 → 按客戶聚齊」。
- 小卡點 1：圖裡用「客戶 1、客戶 2」，但 1.5 的例子用「cust_id」。讀者腦中要自己把「客戶 1」對上「cust_id」。不算大問題。
- 小卡點 2：shuffle 那個圓圈節點 `SH(("shuffle 重新分配"))` 用了雙圓括號（Mermaid 的圓形節點），形狀跟方塊不同。我（不懂 Mermaid）只會看到一個圓形寫著 shuffle，OK，但不知道圓形 vs 方形有沒有特別含意。其實沒有，是純樣式，不影響理解。
- 整體：這張圖比第二張自明得多，因為它配了具體的「客戶」資料。可加強而非缺陷。

---

## 1.5 為什麼 shuffle 是頭號敵人（L108–L128）

**[非常好]** 這節是全章最札實的。用一條真實 SQL（每位客戶這月刷多少）、3000 萬筆、散在幾百 partition、同客戶交易散落各處，然後把 shuffle 拆成 **Shuffle write（寫本機磁碟）** + **Shuffle read（跨網路拉桶）** 兩步，最後點出「幾乎每一筆都經歷一次落地磁碟 + 過網路」。這完全是寫作指引 §3/§4 要的「具體數字落地 + 貫穿範例」。我讀到這裡才真正體會到「貴」是貴在哪。

**[術語沒先定義 — 可加強] L123「分好桶」「桶」**
- 原文：「按 cust_id 分好桶，寫到本機磁碟」「再從其他機器跨網路把對應的桶拉過來」。
- 「桶」是這裡第一次出現，沒解釋。我會想：「桶是什麼？是按 cust_id 分組後的一堆小檔案嗎？」大致能從上下文猜（按 key 分出來的一份份資料），但它是個沒定義的比喻詞。可加強：第一次用「桶」時點一句它指什麼。

**[缺脈絡 — 可加強] L128「資料傾斜」**
- 原文：「最容易出問題（記憶體不夠、資料傾斜）的環節。」
- 「**資料傾斜**」憑空出現、沒解釋。我會想：「傾斜是什麼意思？資料歪掉？」這是一個之後章節大概會展開的重要概念，但在這裡只丟了名詞沒給一句話直覺。
- 建議：要嘛一句話點明（「資料傾斜＝某些 key 的資料量特別大，把某台 executor 壓垮」），要嘛標明「第 0X 章會談」。否則讀者卡在這個沒解釋的詞上。

**[好的] L128 結論句**
- 「CPU 算數很快，但寫磁碟、過網路慢得多」——把為什麼貴的根本原因講白了，誠實且具體。

---

## 1.6 一句話帶走：優化＝少搬、少讀（L132–L143）

**[好的]** 用 boxed 主軸句「讓 Spark 少搬資料、少讀資料」收斂全章，並用編號預告 02–05 章各回答什麼問題（怎麼知道卡在哪 / 怎麼改 SQL / 哪些設定 / 儲存端）。這個「編號 + 一句話 + 指向哪章」的銜接很清楚，符合寫作指引 §10 導覽要求。

**[術語沒先定義 — 真缺陷] L142「AQE」**
- 原文：「哪些 Spark 設定值得調（以及 **AQE** 已經自動幫你處理了哪些 shuffle 問題）？」
- 「AQE」全大寫縮寫，憑空出現、零解釋。我會想：「AQE 是什麼？又一個我不認識的縮寫。」這是全章倒數第二行才砸給我的新術語，而且還暗示它「已經自動幫你處理了 shuffle 問題」——聽起來很重要，但我完全不知道它是什麼。
- 建議：要嘛一句話展開（「AQE，Spark 在跑的時候自動調整的機制」），要嘛這裡先別提、留到第 04 章再引介。在第一章結尾丟一個沒解釋的縮寫，會讓讀者帶著問號離開本章。

**[鷹架洩漏 — 微調] L132「收斂」**
- 原文標題下 L133：「把這章收斂成一條主軸」。
- 寫作指引 §6 明確把「收斂」列為要 grep 的後設旁白關鍵詞。「把這章收斂成一條主軸」偏向作者在描述自己的寫作動作（我要把這章濃縮）。
- 我（讀者）其實不太會卡在這個詞，但既然指引點名要掃，這裡建議改成不帶寫作後設味的講法（例如「這章的重點只有一條主軸」）。屬微調。

---

## 全章主旨一致性

- **一致、且很清楚。** 開頭框承諾「建一個心智模型 + 後面每章都在讓 Spark 少做最貴的事」，1.1→1.5 一路把「partition→平行→shuffle 最貴」這條線拉出來，1.6 用 boxed「少搬、少讀」收口，並接到 02–05 章。前後呼應扎實，沒有過度宣稱（沒有「其實很簡單」式漂亮話）。主軸「資料怎麼切、要不要搬」在 1.1（L35）埋下、1.5 兌現、1.6 定型，是一致的。

## 範疇是否失衡

- **大致平衡，略有頭重腳輕的反向。** 核心（shuffle 為什麼貴）擺在 1.5、給足篇幅與具體例子，這是對的——把篇幅留給核心，符合寫作指引 §1。
- 唯一偏少的是：1.5 結尾才第一次冒出「資料傾斜」、1.6 才冒出「AQE」，兩個都是重要概念卻只有名詞、沒有一句話直覺，等於把它們「丟出來又不接住」。這不算講太多，而是「點到的重要詞沒給最低限度的脈絡」，造成讀者在章末連續撞兩個沒解釋的縮寫/術語（資料傾斜、AQE）。
- 沒有邊緣主題佔版面的問題。CDP/YARN/Hue 這些環境名詞都只是一句帶過，比例合理，問題只在於它們「沒被解釋」而非「佔太多」。

---

## 彙整（三級）

### 真缺陷（必補）

1. **L33「CDP」零解釋** — 憑空出現的平台縮寫，讀者不知道是什麼。第一次出現要一句話說明。
2. **L142「AQE」零解釋** — 全章倒數第二行砸出一個全大寫縮寫，還暗示它很重要（自動處理 shuffle），讀者帶問號離開本章。要嘛一句話展開、要嘛挪到第 04 章再引介。
3. **L59–66 第二張概念圖：Logical Plan / Physical Plan 沒在正文定義** — 圖上 5 個英文方塊，正文「先對上號」清單只解釋了 Catalyst / Stage / Task 三個，獨漏 Logical/Physical Plan。讀者會發現圖文對不齊。
4. **L128「資料傾斜」零解釋** — 重要概念只丟名詞、無一句話直覺，也沒標「第 0X 章談」。

### 可加強（斟酌）

5. **L64 / L71「資料重分配」「搬資料」「shuffle」三種講法未綁定** — 1.3 用「資料重分配/搬資料」、1.4 才正名 shuffle，讀者不確定是不是同一件事。1.3 預告一句「這個搬資料之後叫 shuffle」即可。
6. **L48「Hue」「collect」未點明** — 對純 SQL 讀者，Hue 若點明是「你平常打 SQL 的網頁工具」反而拉近距離；collect 是沒定義的 API 名（有中文「撈回來看」緩衝，可接受）。
7. **L85「key」、L123「桶」未定義** — 都能從上下文猜對，但屬第一次出現未當場定義的詞，各點一句更穩。
8. **L20–31 第一張圖只有 Driver→Executor 單向箭頭** — 文字說 Driver 還要「收結果」，圖上沒反向。讀者會問「結果怎麼回去」。

### 誤讀（不改或微調）

9. **L5「最貴的那件事」先賣關子** — 1.5 才揭曉是 shuffle，屬可接受鋪陳，不改。
10. **L133「收斂」、L74「這正是下一節」屬後設/導覽味** — 讀者其實不太卡，但寫作指引 §6 點名要掃「收斂」，建議微調為不帶寫作動作味的講法。
11. **第三張圖（窄/寬）圓形 shuffle 節點、「客戶1」vs「cust_id」用詞差異** — 不影響理解，微調即可（或不改）。


---

# 第二輪：§1.3 + §1.4

> 第二輪情境：章節剛加入兩節新內容——§1.3「工作分成四層」、§1.4「executor 該多大」（原本的內容整段往後推：現在 §1.5＝兩種運算、§1.6＝為什麼 shuffle、§1.7＝一句話帶走）。我已讀過前半（§1.1、1.2），這輪重點是**這兩節新內容讀不讀得動**＋**加長後整章節奏順不順**。一樣嚴格扮演不懂分散式的 SQL 分析師。
>
> 註：第一輪日誌的小節編號（當時 1.3=計畫怎麼變、1.4=兩種運算…）已與現行章節對不上，那是改版前的狀態，本輪不回頭修正它。

類型標記：缺脈絡 / 太抽象 / 術語沒先定義 / 概念圖不自明 / 太密

---

## §1.3「工作分成四層」（L55–L89）

**[好的 — 四層的「先對齊名字」開場]** L57「先把名字對齊——第 02 章用 Spark UI 找瓶頸時，畫面上看到的就是這四層」。這句很關鍵：它一開始就告訴我「為什麼要記這四個英文詞」——因為之後在工具畫面上會看到它們。對我（要自己營運排程的人）這個動機很實在，我願意記。讚。

**[好的 — 四層各自的定義句]** application/job/stage/task 四條都用粗體點出「決定性的一句」：
- job「每觸發一次 action 就產生一個 job」+「跑三次 count、寫一次表＝四個 job」的數數例子——這個我完全跟得上，因為它直接接 §1.2 剛講完的 action，而且用「數出 4 個」這種能在腦中演算的具體例子。
- task「一個 partition 對應一個 task，100 個 partition 就是 100 個 task」——呼應 §1.1 的 partition，好記。
- 四層由大到小的巢狀關係（application 包 job、job 包 stage、stage 包 task）講得清楚。**這層關係我讀得懂。**

**[術語沒先定義 — 真缺陷] L61 stage 定義裡的「shuffle」前置依賴**
- 原文：「每遇到一次 **shuffle**（下一節會講），就切成下一個 stage。」
- 問題：stage 是四層裡我最需要搞懂的一層（因為它跟效能最相關），但它的定義**整個架在「shuffle」這個詞上**，而 shuffle 要到 §1.5/§1.6 才解釋（這裡寫「下一節」，但下一節其實是 §1.4 executor，shuffle 在更後面的 §1.5/1.6）。
- 我會這樣想：「stage 是『不用搬資料就能連續做完』的一段，遇到 shuffle 切開。可是 shuffle 到底是什麼？我現在還不知道。那我怎麼知道哪裡會『遇到 shuffle』？」——等於 stage 的定義我只能**死記**，沒法真懂，要等讀到 §1.5/1.6 才能回頭補。
- 加上「（下一節會講）」這個指向也不準：下一節（§1.4）講的是 executor 大小，**不是** shuffle。shuffle 的正式解釋在 §1.5、§1.6。讀者照「下一節」翻過去會撲空。
- 我想問：到底哪一節講 shuffle？建議：(a) 指向改準（「§1.5/1.6 會講」或「後面會講」）；(b) 在這裡給 shuffle 一句話最低限度直覺（「shuffle＝把同一個 key 的資料跨機器搬到一起，後面 §1.6 細談」），讓 stage 的定義當場站得住，不必死記。

**[概念圖不自明 — 可加強] L64–73 第一張新圖（四層巢狀圖）**
- 大致看得懂：App 分岔出 Job 1 / Job 2，Job 1 再分出 Stage 1 / Stage 2，Stage 各自再展開成一排 Task。巢狀的「大包小」關係圖上呈現得出來。
- 卡點 1（虛線那一刀）：`S1 -. "中間有 shuffle，切開" .-> S2`，Stage 1 到 Stage 2 之間有一條**虛線**寫「中間有 shuffle，切開」。但同時 S1、S2 又各自**實線**往下指到自己的 Task。我（不懂 Mermaid）會疑惑：S1→S2 那條虛線跟 S1、S2→Task 的實線，是不是代表不同意思的關係？虛線是「切開（順序/邊界）」、實線是「包含（展開成 task）」——這兩種箭頭含意不同，但圖上沒有圖例說明，我得自己猜。而且這條虛線又再次預設我懂 shuffle（同上一條缺陷）。
- 卡點 2（為什麼只有 Job 1 有兩個 stage）：圖裡 Job 1 有 Stage 1+Stage 2，Job 2 卻完全沒畫 stage。我會想：「Job 2 沒有 stage 嗎？還是只是省略沒畫？」——其實是省略，但圖沒明示，讀者可能誤會「有的 job 沒有 stage」。
- 屬可加強：補一句圖說（「虛線＝shuffle 把 stage 切開；實線＝該層展開成下一層」），或在 Job 2 也象徵性畫一個 stage 並標「（省略）」。

**[太抽象／概念圖不自明 — 真缺陷] L77–84 第二張新圖（SQL→Plan 流程圖）與它跟四層的關係**
- 這是本輪我最大的卡點。L75 用「那這四層是怎麼從你的 SQL 變出來的？中間還有一道『優化』」帶出第二張圖：SQL → Logical Plan → Catalyst 優化器 → Physical Plan → 切成 Stage → 每個 Stage 再切成 Task。
- 卡點 A（兩張圖的關係沒講清）：第一張圖是「四層的巢狀結構（誰包誰）」，第二張圖是「從 SQL 一路變成 stage/task 的流程（時間順序/轉換）」。這是兩個**不同視角**——一個是靜態結構、一個是動態流程。但正文沒有點明「這兩張圖在講同一件事的兩個面向」。我會想：「剛剛那張四層圖，跟這張流程圖，是同一個東西嗎？為什麼 stage、task 兩張圖都出現？application、job 在第二張圖怎麼不見了？」——第二張圖**漏掉了 application 和 job 這兩層**（只從 SQL 直接到 Stage、Task），但它前一句又說「這四層是怎麼從 SQL 變出來的」。我會卡在：你說要講四層怎麼來的，可是這張圖只畫了其中兩層（stage/task），另兩層（application/job）跑哪去了？
- 卡點 B（Logical/Physical Plan 又是第一次出現就丟在圖上）：跟第一輪同款問題——Logical Plan、Physical Plan 兩個英文詞在圖上（L79、L81）出現的當下還沒在正文定義，正文要到 L86 才回頭解釋。雖然圖上有「要什麼／實際步驟」的小字，但正式英文名是先出現在圖、後解釋在文。順序反了，讀者第一眼撞到的是沒定義的英文方塊。（好消息：這版 L86 補上了 Logical/Physical Plan 的一句話定義，比第一輪「完全沒定義」進步；只是位置在圖之後。）
- 我想問：能不能在第二張圖之前先講「上面那張是結構、這張是流程」，並且讓流程圖把 application/job 也帶進來（或明說「這張只放大 stage/task 怎麼切，前兩層省略」）？否則兩張圖各看得懂，**彼此的關係我串不起來**。

**[好的] L86–87 Logical/Physical Plan、Catalyst 的正文定義**
- 三個詞都用「你要的結果長什麼樣 vs 實際照什麼步驟做」「自動把過濾條件下推到讀檔」這種能對上 SQL 經驗的話解釋。單看這段是清楚的。問題只在它出現在圖之後（見上條卡點 B）。

**[好的] L89「為什麼要在乎這四層」收尾**
- 「第 02 章你會在 Spark UI 看到 Job 列表，點進去看 Stage，再看 Stage 的 Task 時間分佈。慢在哪一層決定你往哪裡查。」——把四層的實際用途落到「看工具、找瓶頸」，動機收得很好。我讀完知道為什麼要記這四層了。

---

## §1.4「executor 該多大」（L93–L120）—— 重點：讀不讀得動

**整體可讀性評價（先講結論）**：這節**讀得動，但是是全章密度最高、最需要放慢的一節**，而且它的位置（夾在「四層」和「兩種運算/shuffle」中間）讓我有點喘不過氣——它一次拋出 wave、spill、HDFS 吞吐、GC、overhead、廣播 至少六個新詞，其中**好幾個沒當場解釋**。對「未來要自己營運排程」的我來說，這節的內容是我需要的（我認同它該存在、該有深度）；但**就閱讀體驗而言，它現在太密、且放錯位置**——詳見下面逐點與最後的明確建議。

**[好的 — core/memory 拆解與平行度算式]**
- L97–98 把 executor 拆成 core（同時跑幾個 task）和 memory（同時在跑的 task 共用的記憶體），各一句、好懂。
- L102 boxed 算式「同時能跑的 task 數 ＝ executor 台數 × 每台 core 數」——這個算式本身**我完全跟得上**，因為它接 §1.3 剛建立的「一個 task 一個 partition」。

**[太密 — 可加強] L104 wave（批）的例子**
- 原文：「10 台 × 5 core ＝ 同時 50 個 task。若這個 stage 有 200 個 task，得分成大約 4 批（**wave**）才跑得完。」
- wave 這個詞第一次出現，旁邊用「批」對譯，**這個其實夠了**——200÷50＝4 批，我算得出來，跟得上。這條不算缺陷。
- 唯一小建議：可以明說「50 個一批、跑完換下一批，共 4 批」讓 wave＝一輪的意象更實。但現狀可接受。

**[太密 + 術語沒先定義 — 真缺陷] L106–116 胖/瘦表格 + 代價清單一次砸太多新詞**
- 這是 §1.4 讀起來最喘的地方。表格（L108–111）本身好懂：同樣 100 core/400GB，可切成「5 台×20core×80GB（胖）」或「20 台×5core×20GB（瘦）」，這個我跟得上，甚至覺得這個對照表很清楚。
- 但接下來的代價清單（L115–116）在短短兩個 bullet 裡，連續丟出 **spill、HDFS 吞吐、JVM 垃圾回收（GC）、overhead、廣播** 五個新詞，全部沒有當場解釋（只有 GC 有括號中文「垃圾回收」，但「垃圾回收是什麼、為什麼記憶體大就會長停頓」仍沒說）：
  - **spill**（L115①）：「容易 spill 到磁碟」——spill 沒解釋。我會想：「spill 是什麼？溢出？是把記憶體裝不下的資料倒去磁碟嗎？」大致能從「到磁碟」猜，但它是個沒定義的詞。而且「為什麼 spill 不好」也沒明說（要連到 §1.6 才知道寫磁碟很慢）。
  - **HDFS 吞吐**（L115②）：「一台同時對 HDFS 開太多讀取，吞吐反而卡住」——HDFS 是什麼？前面（§1.1）完全沒出現過 HDFS。我會想：「HDFS 是放資料的地方嗎？」（猜得到是儲存層，但沒被介紹過）。「吞吐」也是新詞。這一整條對我幾乎是黑盒。
  - **JVM 垃圾回收（GC）**（L115③）：「記憶體開到很大時，JVM 的垃圾回收（GC）會出現長停頓」——這條對沒學過分散式/JVM 的我**最黑盒**。JVM 是什麼？垃圾回收又是什麼？為什麼記憶體大就會「長停頓」？三個為什麼疊在一句裡，我完全接不住，只能整條跳過。
  - **overhead**（L116①）：「每台都要保留一塊固定的『管理用』記憶體（overhead）」——這條反而解釋得最好（用「管理用」中文點明了），我能懂「台數越多、被這塊吃掉越多」。這條 OK。
  - **廣播**（L116②）：「廣播出去的小表（第 03 章會講）要每台各複製一份」——廣播沒解釋，但**有標「第 03 章會講」**，這個處理方式是對的：我知道它是後面的主題，現在先記「會複製到每台、台數多就費記憶體」即可。這條可接受。
- 歸納：同一個清單裡，**overhead、廣播處理得當（中文點明 or 標章節），但 spill、HDFS 吞吐、JVM GC 三個是「丟出來沒接住」**，尤其 GC 那條三層黑盒最勸退。對一個不懂分散式的讀者，這三條會讓我從「跟得上」掉到「這段我讀不懂、先跳過」。

**[太密 — 真缺陷] L106→L118 整節沒有喘息點**
- 從 L106 開始一路是：取捨前提 → 表格 → 太胖四個代價 → 太瘦兩個代價 → L118 起手建議（4~5 core）→ L120 收尾指向 04 章。中間**沒有任何一個具體小例子或停頓**讓我消化「為什麼搶記憶體會 spill」「為什麼台數多 overhead 就多」。代價清單是純列點的抽象因果（A 導致 B），不像 §1.6 那樣有「3000 萬筆每筆都過一次網路」的具體畫面。
- 對照：§1.6 講 shuffle 為什麼貴，是先給一條 SQL、給 3000 萬筆的數字、再拆 write/read 兩步——有畫面、有數字、好吸收。§1.4 的代價清單**沒有這種落地**，全是「太胖會①②③④」的乾貨因果。這就是它「太密」的根源：資訊量大、但缺具體錨點。

**[好的] L118 起手建議 + L120 收尾**
- L118「每台 executor 抓大約 4~5 個 core」給了一個可帶走的具體數字結論，誠實（「常見的起手建議」不誇大）。
- L120「先有直覺就好…第 04 章詳談…而且很多時候不如先照第 03 章把 SQL 寫法弄對」——這個收尾很好，它**主動幫讀者卸下壓力**（不用現在記死這些數字），還把優先序講對（SQL 寫法 > 糾結 executor 數字）。這對讀完一節重內容的我是很需要的喘息。

---

## §1.4 是否「太進階以致一般讀者讀不動」——明確評價與建議

**評價：不是「太進階讀不動」，而是「進階方向對、但呈現太密、且位置打斷主線」。** 拆三點：

1. **內容深度本身 OK、該留。** 對「未來要自己營運排程、經營特徵庫」的目標讀者，executor 大小取捨是真的需要的知識，不該砍。算式、胖/瘦表格、4~5 core 結論都是好的、可帶走的。問題不在「太深」。

2. **真正讀不動的是 L115 那三個沒接住的詞（spill / HDFS 吞吐 / JVM GC）。** 這三個是讓「跟得上」掉到「跳過」的斷點。建議：
   - **spill**：當場一句話（「spill＝記憶體裝不下，只好把資料倒去磁碟，而磁碟慢」）——順便預埋「磁碟比記憶體慢」這個 §1.6 會用到的直覺。
   - **HDFS**：第一次出現點一句它是什麼（「HDFS＝叢集存放資料的地方，§1.1 那些 partition 就放在這」），把它接回 §1.1 已建立的概念。
   - **JVM GC**：這條對本讀者最難。建議**大幅簡化或降級**——要嘛砍掉只留「記憶體開太大反而可能有額外的管理停頓（後面章節談）」，要嘛用一句完全不靠 JVM 知識的白話（「記憶體太大時，系統定期整理記憶體會卡頓一下」）。現狀「JVM 的垃圾回收會出現長停頓」對沒學過的人是三層黑盒，是整節最該動的一句。

3. **建議拆段 + 加一個小例子（針對「太密」）：**
   - **拆段**：把 L115「太胖」那一條（四個代價）拆成「記憶體面（搶記憶體→spill）」和「系統面（HDFS 吞吐、GC）」兩小群，中間空一行喘口氣；或把代價清單從密集 bullet 改成「先講一句總原則，再列點」。
   - **加小例子**：在「搶同一份記憶體→spill」這條，補一個跟 §1.4 算式呼應的迷你數字例（「80GB 給 20 個 task 分，平均一個 task 只有 4GB；同樣 80GB 但只跑 5 個 task，一個就有 16GB」），讓「胖→每個 task 分到的少→容易 spill」有畫面。這正是這節現在缺的「落地錨點」。

4. **位置（節奏）問題——這條比上面更值得考慮：** §1.4 夾在 §1.3（四層）和 §1.5/1.6（shuffle）之間，但它**深度依賴 shuffle/spill 的直覺（為什麼 spill 不好＝磁碟慢，這要 §1.6 才講）**，卻排在 shuffle 之前。導致 §1.4 一直在用「之後才解釋的東西」（shuffle、spill、磁碟慢）。我讀的時候會覺得：「為什麼把最硬、又依賴後面知識的一節，插在我還沒學會 shuffle 的時候？」——建議評估**把 §1.4 整節移到 shuffle（現 §1.6）之後**，這樣 spill「為什麼不好」、shuffle「廣播為什麼省」都有了根基，§1.4 會好讀非常多。若不便移動，至少在 §1.4 開頭加一句「這節會先用到後面才細講的 shuffle/磁碟慢的直覺，看不懂的詞先記個大概，§1.6 會補」。

---

## 加了兩節後的整章節奏

**[頭重腳輕的反向 — 中段過重]** 加入 §1.3、§1.4 後，章節的重量明顯**壓在中段**：§1.1、§1.2 是輕快的鋪陳（一群機器、lazy），到 §1.3 開始上難度（四層＋兩張圖），§1.4 是全章密度峰值（executor 取捨＋一堆新詞），然後 §1.5（兩種運算）、§1.6（shuffle 為什麼貴）又是另一個重點高峰。等於**連續三節（1.3→1.4→1.5/1.6）都是高負載**，中間沒有緩坡。對讀者是：前面很順、突然連爬三段陡坡。

**[節奏卡點 — §1.4 與 §1.5/1.6 的順序錯位]**（同上節第 4 點，但從整章節奏角度再記一次）：現在的順序是「四層(1.3) → executor 大小(1.4) → 兩種運算(1.5) → shuffle 為什麼貴(1.6)」。但 §1.4 講 executor 取捨時，反覆要用到 spill、shuffle、磁碟比記憶體慢這些**要到 §1.6 才建立**的直覺。讀者在 §1.4 等於先還債（用了還沒學的概念）、到 §1.6 才補課。**更順的順序可能是：1.3 四層 → 1.5 兩種運算 → 1.6 shuffle 為什麼貴 → 然後才 1.4 executor 該多大**（把資源取捨放在 shuffle 直覺建立之後）。這樣 §1.4 的每個代價（spill、廣播）都有前面剛建立的根基可接。

**[好的 — 四層這節插得是對位置的]** §1.3 四層接在 §1.2 action 之後是順的：§1.2 剛講「action 觸發才跑」，§1.3 馬上說「每個 action 產生一個 job」，銜接自然。四層這節的位置沒問題，問題集中在 §1.4。

---

## 第二輪彙整（三級）

### 真缺陷（必補）

1. **§1.3 L61 stage 定義架在未解釋的「shuffle」上，且「（下一節會講）」指向錯誤**（術語沒先定義）—— stage 是最該懂的一層卻只能死記；且下一節（§1.4）講的不是 shuffle，shuffle 在 §1.5/1.6。修：指向改準＋給 shuffle 一句話直覺讓 stage 定義當場站得住。
2. **§1.3 L77–84 第二張圖與第一張圖、與「四層」的關係沒串起來**（概念圖不自明／太抽象）—— 兩張圖一個是結構、一個是流程，正文沒點明；且第二張流程圖漏了 application/job 兩層，卻接在「四層怎麼從 SQL 變出來」之後。修：圖前一句點明「結構 vs 流程」，並讓流程圖帶上或明確省略前兩層。
3. **§1.4 L115 三個沒接住的新詞：spill / HDFS 吞吐 / JVM GC**（術語沒先定義）—— 這是 §1.4 從「跟得上」掉到「跳過」的斷點，尤其 JVM GC 是三層黑盒。修：spill、HDFS 各補一句白話並接回已有概念；JVM GC 大幅簡化或降級為不靠 JVM 知識的白話。
4. **§1.4 整節太密、無落地錨點**（太密）—— 代價清單是純抽象因果，缺 §1.6 那種具體數字畫面。修：拆段（記憶體面／系統面）＋補一個「80GB 分給 20 vs 5 個 task」的迷你數字例。

### 可加強（斟酌）

5. **§1.4 位置/整章節奏：executor 取捨排在 shuffle 之前，卻依賴 shuffle/spill 的直覺**（太密/節奏）—— 評估把 §1.4 移到 shuffle（現 §1.6）之後；若不移，§1.4 開頭加一句「會先用到後面才細講的詞，先記大概」。中段連續三節高負載（1.3→1.4→1.5/1.6）缺緩坡。
6. **§1.3 L64–73 第一張圖缺圖例**（概念圖不自明）—— 虛線（shuffle 切開）vs 實線（展開成 task）含意不同卻無說明；Job 2 沒畫 stage 易誤會「有的 job 沒 stage」。修：補一句圖說、Job 2 象徵性補一個 stage 標「省略」。
7. **§1.3 L79/L81 Logical/Physical Plan 仍是「先出現在圖、後解釋在文」**（術語沒先定義）—— 比第一輪進步（L86 已補定義），但順序仍反。修：把定義移到圖前，或圖上改用中文。
8. **§1.4 L104 wave**（太密，輕微）—— 「批」對譯已夠，可選擇性加「一批 50 個、跑完換下一批」讓意象更實。現狀可接受。

### 誤讀（不改或微調）

9. **§1.4 GC 的括號中文「垃圾回收」** —— 有對譯是好的，但對譯不等於解釋（為什麼記憶體大就停頓仍沒說）；歸到上面第 3 點一起處理，不單獨改。
10. **第一張新圖的圓形/方形節點** —— 同第一輪結論，純樣式不影響理解，不改。


---

# 第三輪（重跑）：§1.2 / §1.6 / §1.8 / §1.9 + 整體節奏

> 第三輪情境：章節再次擴充成 **10 節**，且結構已重排——對照第二輪建議，可見：(a) partition 從哪來獨立成 **§1.2**（新節）；(b) 四層變 **§1.4**；(c) executor 該多大已**移到 shuffle 之後變 §1.7**（呼應第二輪「把資源取捨放到 shuffle 直覺建立之後」的建議）；(d) 新增 **§1.8 一條 SQL 的旅程**、**§1.9 為什麼比老 Hive 快**。本輪重點：§1.2 / §1.6 / §1.8 / §1.9 四節讀不讀得動 + 擴成 10 節後整章節奏。一樣嚴格扮演不懂分散式的銀行 SQL 分析師。
> 註：前兩輪的小節編號已與現行對不上（現行 §1.2=partition 從哪來、§1.5=兩種運算、§1.6=shuffle 為什麼貴、§1.7=executor）。本輪用現行編號，不回頭修舊輪。

類型標記：缺脈絡 / 太抽象 / 術語沒先定義 / 概念圖不自明 / 太密 / 節奏

---

## §1.2「你的資料被切成幾塊？partition 從哪來」（L39–L56）

**[好的 — 這節讀得懂，是新增節裡最成功的]** 這節我跟得上，整體評價正面。核心問句「切成幾塊」一開頭就點明「**一個 partition 對應一個 task，所以 partition 數＝這步最多能同時做幾件事**」（L41），把抽象的 partition 數綁到我能感受的「能同時做幾件事」，這個 framing 很好。

**[好的 — 128MB 與「30GB÷128MB≈240」的數字]**
- L43「一塊預設約 **128MB**（由 `spark.sql.files.maxPartitionBytes` 控制）」+「30GB ÷ 128MB ≈ **240 個 partition → 240 個 task** 平行讀」——**這個算式對我幫助很大**。它把「partition 數從哪來」從玄學變成一個我能自己用除法估的東西。我會想：「喔，所以我那張幾十 GB 的表，大概會被切成兩三百塊、兩三百個 task 一起讀」——這是我第一次能對「平行度」有量感。讚。
- L43 括號「這是粗估：很多小檔、或像 gzip 這種『不能從中間切開』的檔，實際塊數會不一樣」——這個誠實補充很好，不會讓我把 240 當成鐵律。唯一小卡點：「gzip 不能從中間切開」對我是新資訊，但它只是舉例、不影響主軸，可接受（我會想「喔原來壓縮檔有差」但不會卡住）。

**[好的 — 太少/太多取捨]**
- L47 太少：「平行度不足，機器在閒著」+ 極端「整張大表只有 1 個 partition＝一個 task 扛全部，叢集再大也沒用」——這個極端例子很有畫面，我立刻懂「切太大＝浪費叢集」。
- L48 太多：「每個 task 都有固定的啟動、排程開銷；幾萬個只有幾 KB 的 task，光開銷就拖垮你」——這個我也懂，且呼應了「寫出時一堆小檔」。兩頭取捨講得對稱、清楚。
- **這節的取捨我讀得懂**，因為兩頭都給了「會發生什麼壞事」的具體後果（閒置 vs 開銷），不是乾巴巴的「太多太少都不好」。

**[術語沒先定義 — 真缺陷] L47「spill」在 §1.2 首次出現、但解釋在 §1.6**
- L47 太少那條：「單一 task 要嚼的資料太多，記憶體不夠就 **spill** 到磁碟」——這是全章 **spill 第一次出現的地方**（比第二輪時的 §1.7 更早），而 spill 的正式解釋（「記憶體裝不下，溢寫到磁碟」）要到 §1.6 L164 才出現。
- 我會這樣想：「spill 是什麼？溢出？是把記憶體裝不下的東西倒去磁碟嗎？」——大致能從「記憶體不夠就 spill 到磁碟」這半句猜出意思（這版把 spill 跟「記憶體不夠…到磁碟」綁在同一句，比裸丟一個 spill 好），但它仍是個**沒當場定義的英文詞**，而且「為什麼 spill 不好」（磁碟慢）要到 §1.6 才知道。
- 嚴格扮演下我會標：這是術語提前出現、解釋滯後。**不算最嚴重**（因為同句的「記憶體不夠…到磁碟」已給了七成直覺），但既然 §1.6 L164 才正式說「記住 spill 這個詞」，這裡 §1.2 就先用了，順序上 spill 是「先用、後正式定義」。
- 我會問/建議：§1.2 這裡 spill 首次出現時補半句「（spill＝記憶體裝不下，只好倒去磁碟，而磁碟慢，§1.6 細談）」，或至少標「§1.6 會講」。否則 §1.6 L164「記住 spill 這個詞，後面幾節會一直用到」這句話的時序是反的——讀者在 §1.2 就已經撞過它了。

**[概念圖不自明 — 可加強] L50–54 partition→task 流程圖**
- 圖很簡單好懂：30GB 表 →（按 ~128MB 切）≈240 partition → 240 task 平行讀。一條線、三個方塊，自明。這張圖**我一眼就懂**，沒有第二輪那種「兩張圖關係串不起來」的問題。
- 唯一小事：圖把 §1.2 正文的數字（30GB、128MB、240）原樣重現，等於圖文重複。對我這種讀者反而是好事（再確認一次），不算缺陷。

**[好的 — L56 收尾「partition 數不是固定的」]**
- 「讀進來是一個數，經過 shuffle 後會變成另一個數（§1.6 會講），自己重新分配也會變」——這句很重要，它先打預防針，讓我之後在 §1.6 看到「shuffle 後變 200」時不會以為矛盾。而且這次**指向是準的（§1.6）**，不像第二輪那個指錯的「下一節」。讚。

---

## §1.6「為什麼 shuffle 是頭號敵人」三麻煩（L144–L168）

**整體評價（先講結論）**：這節**三塊不算太多太雜，反而我覺得收得不錯**——因為三塊各自對應一個我會真的遇到的痛（記憶體爆、卡在最慢的人、為什麼老是 200），標題「麻煩一/二/三」也讓我知道現在讀到第幾塊。比第二輪的 §1.7（一個 bullet 砸五個詞）好讀很多。逐塊評價如下。

**[非常好 — shuffle write/read 的拆解仍是全章最札實]**
- L155–160 把 `GROUP BY cust_id` 為什麼要 shuffle 講得很清楚：3000 萬筆、同客戶散落各處、必須搬到同一地方才能加總，拆成 **Shuffle write（序列化→寫本機磁碟）** + **Shuffle read（跨網路拉）** 兩步，最後「幾乎每一筆都經歷一次序列化→落地磁碟→過網路」。這段我讀到才真正體會「貴」貴在哪。
- 注意這版 L157「序列化＝把資料轉成可傳輸的位元組」**當場給了括號解釋**——比舊版進步，序列化不再是黑盒。讚。

**[麻煩一：spill — 好的]** L164「記憶體塞不下，Spark 只好把一部分**溢寫到磁碟（spill，溢寫）**，又被磁碟拖慢一次」——這裡 spill 終於有了「溢寫」中文對譯＋「被磁碟拖慢」的後果，**這句本身是清楚的**。問題只在它跟 §1.2 L47 的時序（見上面 §1.2 那條缺陷）：L164「記住 spill 這個詞，後面幾節會一直用到」暗示這是 spill 首次登場，但其實 §1.2 已用過。建議兩處擇一調整時序。

**[麻煩二：stage barrier + skew — 非常好，那個畫面很清楚]**
- L166「一個 stage 的所有 task 沒有全部跑完，下一個 stage 一個都不能開始——因為 shuffle read 必須等所有 shuffle write 都寫好了才能拉」——**「等齊」這個機制我懂了**，而且它解釋了為什麼一個慢 task 會拖垮全部。
- 關於那個畫面：「**199 個 task 三秒做完，剩 1 個肥 task 跑了五分鐘，其他人只能乾等它**」——**這個畫面非常清楚、非常有感**。我立刻在腦中看到 199 個人做完在旁邊乾等 1 個累死的人。這是本節最好的一句，把「資料傾斜為什麼痛」用一個畫面釘死了。而且這裡「資料傾斜（skew）」當場給了直覺（某些 key 資料量特別大、某超級大戶、或一個 NULL key 吃掉一大塊），**比第一輪「資料傾斜憑空出現沒解釋」大幅進步**——這個第一輪的缺陷這版補上了。
- 小卡點（可加強）：那個畫面用「199 + 1 = 200」，而「為什麼是 200」要到下一塊（麻煩三）才解釋。讀者在麻煩二看到 199/200 時可能短暫想「為什麼剛好 200？」然後在麻煩三得到答案。順序上其實 OK（很快就接住了），但若要更順，麻煩二這句可改成「199 個 task…剩 1 個」不寫死 200，或加半句「（為什麼是 200？下一塊講）」。屬非常輕微的微調。

**[麻煩三：為什麼是 200 — 懂了，且解釋得好]**
- L168 把「shuffle 後永遠 200 個 task」的困惑解掉了：「shuffle 輸出的 partition 數**不是延續輸入**，而是被重設成固定值 `spark.sql.shuffle.partitions`，**預設 200**」——**「為什麼是 200」我懂了**。關鍵是它點明「不是延續輸入、是被重設」，這正好回收了 §1.2 L56「shuffle 後會變成另一個數」那句預告，前後呼應很扎實。
- L168 還講了 200 太大太小的後果（小結果＝200 個空 task＋小檔；大結果＝每塊太大狂 spill）+ AQE 會自動合併過小的——這個「你多半不必手動煩惱，但要看得懂為什麼是 200」的定位很對。
- **[術語沒先定義 — 可加強] L168「AQE」**：AQE 在這裡（§1.6）第一次出現，括號「Spark 3.3 的 AQE 會在執行時自動把過多的小 partition 合併」算是給了一句功能描述，**比第一輪「AQE 零解釋」進步**。但 AQE 這個縮寫本身展開成什麼（Adaptive Query Execution）要到 §1.10 L268 才出現。我會想「AQE 三個字母是什麼的縮寫？」——不影響理解（我知道它是個自動合併的機制就夠用了），屬可加強。建議 §1.6 首次出現時順手給全名。
- L168 結尾括號「AQE 主要幫『塊太小太多』這頭把它們併大；『塊太大』那頭得靠別的辦法」——這個誠實邊界很好，避免我以為 AQE 萬能。

**三麻煩會不會太多太雜？——明確評價：不會。** 三塊各打一個不同的痛點（spill＝記憶體、barrier/skew＝等最慢的人、200＝塊數重設），彼此不重複、有遞進（都從「shuffle 要搬資料」這個根長出來）。「麻煩一/二/三」的標號讓我知道進度。唯一要注意的是這節**本身已經是全章第二密的一節**（僅次於 §1.7），三麻煩＋前面的 write/read 拆解＋3000 萬筆例子，資訊量大；但因為每塊都有畫面或具體數字（不像 §1.7 的乾因果），讀起來不喘。**維持三塊即可，不需要砍。**

---

## §1.8「一條 SQL 的旅程」+ stage DAG 圖（L205–L244）

**整體評價（先講結論）**：**這節是整章「最有收穫」的一節，不是太複雜。** 它把前面所有零件（partition、窄/寬、shuffle、stage、task）用一條我天天會寫的 SQL（JOIN + GROUP BY）串起來，是「啊原來連起來是長這樣」的拼圖時刻。對「未來要自己看執行計畫找瓶頸」的我，這節的價值最高。逐點如下。

**[非常好 — 用一條真實 SQL 收束全部零件]**
- L209–215 那條 SQL（`JOIN dim_customer ON cust_id` + `WHERE month` + `GROUP BY segment`）是我天天寫的形狀，**親切、立刻能對上**。用它當「旅程」的主角，比抽象講「假設有兩個 shuffle」好太多。

**[四個 stage 跟得上嗎？——跟得上，但需要對著圖慢讀一次]**
- L219–236 的 stage DAG 圖：Stage1（讀 card_txn→WHERE→按 cust_id shuffle write）、Stage2（讀 dim_customer→按 cust_id shuffle write）、Stage3（shuffle read 對齊兩邊→JOIN→按 segment shuffle write）、Stage4（shuffle read→SUM→寫出）。
- **四個 stage 我跟得上**，但**不是一眼，是對著 L238 的「讀法」逐條比對才串起來的**。第一眼看圖我會有兩個瞬間困惑：
  - 困惑 1（為什麼有兩個入口 stage）：S1 和 S2 並排、各自做完再一起流進 S3。我一開始會想「為什麼 dim_customer 要單獨一個 stage？」——讀到 L238 第 2 點「JOIN 是一次 shuffle」+ 想起 §1.5「JOIN 兩邊相同 key 的列要碰頭」才懂：**兩張表都要各自按 cust_id shuffle write，所以各占一個 stage，在 S3 才碰頭**。這個「JOIN＝兩個輸入 stage 匯流」對我是新畫面，圖畫出來了、但要靠讀法補一句才理解。
  - 困惑 2（shuffle write 在這個 stage 尾、shuffle read 在下個 stage 頭）：圖上 S1 結尾是「shuffle write」、S3 開頭是「shuffle read」。我得自己意識到「S1 的 write 和 S3 的 read 是同一次 shuffle 的兩半，被 stage 邊界切開」——這正是 §1.6 講的 write/read 兩步，但圖上 write 和 read 分屬不同 stage 方塊，要自己連線。讀法 L241 有點到「每個 shuffle 都是序列化→落地→過網路」，但**沒明說「write 在上個 stage 尾、read 在下個 stage 頭、合起來是一次 shuffle」**。
- 建議（可加強）：圖或讀法補一句把「write/read 跨 stage 邊界配對」點明，例如「每條 stage 之間的 write→read 箭頭，就是 §1.6 那次 shuffle 的兩半」。補上後這張圖會從「跟得上」升到「一眼懂」。

**[好的 — 讀法三點收束]** L238–242 三點讀法：①WHERE 窄依賴+partition 裁剪、②JOIN 和 GROUP BY 各一次 shuffle→兩個 shuffle 切多個 stage、③每 stage 的 task 數＝partition 數（240 / 200）。這三點把圖翻譯成「成本在哪、幾個 task」，**和圖配著讀就完整了**。第 3 點還回收了 §1.2 的 240 和 §1.6 的 200，前後呼應扎實。

**[非常好 — 「小表 broadcast 免一個 shuffle」有沒有打到「寫法差很多」的點？——打到了，而且打得很準]**
- L244「如果 dim_customer 其實很小（幾 MB 的客群對照表），Spark 可以把它**廣播**到每台 executor，JOIN 就地完成、**完全不用為 join 做 shuffle**——於是少掉一整個 shuffle、少切好幾個 stage」——**這段精準打到「同一條 SQL、寫法/條件不同，成本差很多」的點。** 因為前面剛用圖讓我看到「JOIN＝多一個 shuffle＝多切 stage」，這裡馬上說「小表廣播就能把這整個 shuffle 拿掉」，我**直接在剛建立的圖上做減法**：S2 整個沒了、S3 不用 shuffle read 對齊——這個對比非常有感。這是我讀完最想記住的一句。
- 唯一小卡點（可加強）：「廣播」這個詞在這裡（§1.8）算第一次實質出現（§1.7 L197 提過「廣播出去的小表」但沒展開），這裡也只說「廣播到每台 executor、就地完成」+標「第 03 章 broadcast join」。對我**夠用**（我知道是「把小表複製到每台、就地 join、免 shuffle」），但「廣播」到底是 Spark 自動做還是我要寫什麼，這裡沒說（標了第 03 章，可接受）。我會問「我怎麼讓它廣播？是自動的嗎？」——但既然指了第 03 章，這個延後是合理的。

**這節是不是整章最有收穫的？——是。** 對我（要營運排程、看計畫找瓶頸）來說，§1.8 把零件組成一張我能對著真實 SQL 看的圖，且用 broadcast 對比示範「寫法差很多」，收穫密度最高。**它不複雜，是「把已學的東西拼起來」，所以即使資訊多也不累**——前提是前面 §1.1–§1.6 的零件都學會了（這也是為什麼它放在第 8 節是對的）。

---

## §1.9「為什麼 Spark 比老 Hive 快」（L248–L254）

**整體評價（先講結論）**：**對同時用 Hive 的我有用，而且看得懂。** 這節短、聚焦、誠實，是很舒服的一節。

**[好的 — 對同時用 Hive 的我有用]**
- 開頭 L250「你可能也用 Hive 跑過 SQL」直接認領我的處境——我**就是**在 Hue 上有時跑 Hive、有時跑 Spark 的人，這節等於回答我心裡一直有的「那這兩個到底差在哪、為什麼有人說 Spark 快」。這個切入很貼我。

**[好的 — 「job 之間落地 HDFS vs 記憶體 pipeline」看得懂嗎？——看得懂]**
- L250「老式 Hive 跑在 MapReduce 上…一條多步驟查詢被拆成一個個 MapReduce job，**每個 job 之間都把整批中間結果落地到 HDFS**，下一個 job 再從 HDFS 讀回來——多步驟就是多次『寫 HDFS→讀 HDFS』的來回」——**這個我懂**，因為它跟 §1.6 剛建立的「寫磁碟/過網路很慢」直覺對上了：原來老 Hive 是每一步都付一次這個慢成本。
- L252 Spark 的對比「整條查詢規劃成一張 **DAG**（上一節那條 SQL 的 stage 圖就是），一個 stage 內多個窄依賴在記憶體裡串著做、中間不落地，只有遇到 shuffle 才寫磁碟」——**這段是這節的核心，我看得懂**，關鍵是它**回指了 §1.8 那張 stage 圖**（「上一節那條 SQL 的 stage 圖就是 DAG」），讓「DAG」這個新詞不是憑空來的，而是「你剛看過的那張圖」。這個回指很聰明，省了我消化一個新名詞。
- 對比落點清楚：**老 Hive＝每步都寫讀 HDFS；Spark＝只有 shuffle 才落地，窄依賴串在記憶體**。我讀完能用一句話複述差別，這就是看得懂的證明。

**[術語沒先定義 — 可加強] L250「MapReduce」、L252「DAG」**
- 「MapReduce」：當作「老式 Hive 跑的引擎」的專有名詞用，沒展開是什麼。對我**不影響理解**（我只需要知道它是老 Hive 底層、特性是每步落地 HDFS），可接受。但嚴格標：它是個沒解釋的專有名詞。
- 「DAG」：L252 有「規劃成一張 DAG（上一節那條 SQL 的 stage 圖就是）」——用「就是上一節那張圖」實質解釋了 DAG 長什麼樣，**等於給了直覺**，比裸丟縮寫好。但 DAG 三個字母展開（有向無環圖）沒給。對我夠用（我知道它＝那張 stage 流程圖），屬可加強。

**[非常好 — L254「別過度宣稱」很誠實]**
- L254「Spark 不是『永遠比較快』。對非常單純的大批次掃描差距有限；而且**現在的 Hive 多半跑在 Tez 上（不是 MapReduce）**，Tez 也用 DAG、把差距縮小了不少」——**這段我很欣賞**。它沒有把 Spark 捧成萬能，還主動告訴我「你現在用的 Hive 可能已經是 Tez、不是我前面打的那個老 MapReduce 了」。這對我很重要：否則我會拿這節去貶低我同事還在用的 Hive，結果發現我們的 Hive 是 Tez、根本沒那麼慢。這個誠實避免了我誤用本節結論。
- 小卡點（可加強）：「Tez」又是一個新專有名詞、沒解釋。但它和 MapReduce 一樣只是「Hive 的某個底層引擎」的名字，不影響主軸，可接受。標一下而已。

---

## 整章節奏（擴成 10 節後，從頭到尾）

**[長度判斷：偏長，但「該留」——不是冗，是密度分佈不均]**
擴成 10 節後，**長度偏長**（單章從頭讀到尾要相當專注），但我**不主張大砍**——因為內容大多是我（要營運排程的人）需要的，且 §1.8/§1.9 是新增的高價值節。問題不在「總量太多」，而在**密度分佈不均 + 有兩處可瘦身**（見下）。

**[節奏地圖 — 從頭到尾的負載曲線]**
- §1.1 一群機器：**輕**、好的暖身。
- §1.2 partition 從哪來：**中**、新節但好讀（128MB/240 那個算式幫了大忙）。
- §1.3 lazy/action：**輕**、喘息點。
- §1.4 四層：**中偏重**（兩張圖、前兩輪的圖關係問題本輪沒專審但仍在）。
- §1.5 兩種運算：**中**。
- §1.6 shuffle 為什麼貴（三麻煩）：**重**（全章第二密，但有畫面，不喘）。
- §1.7 executor 該多大：**最重**（全章密度峰值，仍是 spill/HDFS/GC 那串）。
- §1.8 一條 SQL 的旅程：**重但好吸收**（拼圖時刻，資訊多但都是已學的東西組合）。
- §1.9 為什麼比老 Hive 快：**輕**、舒服的下坡，是很好的喘息。
- §1.10 一句話帶走：**輕**、收束。

**[節奏判斷 1 — 移動 §1.7 是對的，中段不再「連三陡坡」]**
第二輪我抱怨「1.3→1.4→1.5/1.6 連續三節高負載、無緩坡」，而且 executor 排在 shuffle 之前、依賴還沒學的 spill。**這版把 executor 移到 §1.7（shuffle 之後），這個改動我很有感**：現在 §1.7 講 spill 時，§1.6 剛建立完「spill＝溢寫磁碟、磁碟慢」，§1.7 不再憑空用未學的詞。順序對了。

**[節奏判斷 2 — 新的負載峰值挪到了 §1.6→§1.7，§1.7 仍是最該喘的地方]**
但移動的代價是：現在 **§1.6（三麻煩，重）緊接 §1.7（executor，最重）**，這兩節連著是全章最硬的一段。§1.6 因為有畫面還好，**§1.7 仍是整章最該放慢、也最該瘦身的一節**——第二輪點的 spill/HDFS 吞吐/JVM GC 三個黑盒詞（本輪沒重審 §1.7 內文，但從節奏看它仍是峰值）。**若要精簡，我第一個動的就是 §1.7**：把 JVM GC 那條降級或砍掉、代價清單拆成「記憶體面/系統面」。這條沿用第二輪建議。

**[節奏判斷 3 — §1.8/§1.9/§1.10 收尾三節節奏很好]**
§1.8（拼圖，重但好吸收）→ §1.9（比 Hive 快，輕）→ §1.10（一句話，輕）這個收尾曲線**很舒服**：先用 §1.8 把所有東西拼起來達到「理解高潮」，再用 §1.9/§1.10 緩降收束。**§1.9 在這裡正好是 §1.8 高潮後的喘息**，位置很對。這三節不要動。

**[哪裡該喘口氣 / 太密 / 重複 — 明確指認]**
- **太密、最該動：§1.7**（同上，沿用第二輪）。它夾在 §1.6（重）和 §1.8（重）之間，三節連著是全章唯一讓我想中途停下來喝口水的地方。**若要在 §1.6 和 §1.7 之間插一個喘息**，可考慮把 §1.7 開頭的「知道 shuffle 會 spill、磁碟很慢之後」那句承上做得更像一個小結停頓。
- **輕微重複（可瘦身，但不急）**：
  - 「240」這個數字出現在 §1.2（算式）、§1.2 圖、§1.8 圖、§1.8 讀法第 3 點，共四次；「200」出現在 §1.6 麻煩三、§1.7 wave 例子、§1.8 讀法第 3 點。對我這種讀者，**重複其實是強化、利大於弊**，但若編輯要砍字，這些回指可以精簡（例如 §1.8 讀法第 3 點不必再解釋 240/200 從哪來，只說「§1.2 的 240、§1.6 的 200」）。
  - spill 在 §1.2、§1.6、§1.7 各出現，但這是**時序問題（首次在 §1.2 卻晚到 §1.6 才定義）而非單純重複**，已在 §1.2 缺陷條記。
- **不該再加東西**：10 節對單章已到上限。任何新概念建議往後面章節放，別再撐 §01。

**[整章主旨一致性 — 仍然一致、且更完整]**
開頭框「建心智模型→後面每章讓 Spark 少做最貴的事」，到 §1.10 boxed「少搬、少讀、用對機器」收束（注意這版多了「**用對機器**」第三條，呼應了新增的 §1.7 executor——主軸隨章節擴充也擴成三條，一致）。§1.8 是這條主軸的「實證」、§1.9 是「跟舊工具對比的佐證」，都服務主軸，沒有跑題。**擴成 10 節後主旨不但沒散，反而因為 §1.8 的串接而更完整。**

---

## 第三輪彙整（三級）

### 真缺陷（必補）

1. **§1.2 L47「spill」首次出現早於 §1.6 的正式定義，時序反了**（術語沒先定義）—— spill 全章最早出現在 §1.2，但 §1.6 L164 才說「記住 spill 這個詞，後面會一直用到」（暗示首次登場）。同句「記憶體不夠就 spill 到磁碟」已給七成直覺，故非最嚴重，但建議 §1.2 首次出現補半句定義或標「§1.6 細談」，並把 §1.6 L164「記住這個詞」的措辭改成不暗示首次。
2. **§1.8 stage 圖：shuffle write/read 跨 stage 邊界配對沒點明**（概念圖不自明）—— 四個 stage 我跟得上但非一眼，最大卡點是「S1 尾的 write 和 S3 頭的 read 是同一次 shuffle 的兩半」要自己連線。修：圖或讀法補一句「stage 之間的 write→read 就是 §1.6 那次 shuffle 的兩半」。補上後這張（全章最有價值的）圖會從「跟得上」升到「一眼懂」。

### 可加強（斟酌）

3. **§1.6 / §1.10 AQE 縮寫全名滯後**（術語沒先定義）—— AQE 在 §1.6 首次出現只給功能描述，全名 Adaptive Query Execution 要到 §1.10 才出現。比第一輪「零解釋」已大幅進步，建議 §1.6 首次出現順手給全名。
4. **§1.8 stage 圖兩個入口 stage（JOIN＝兩表各 shuffle write 匯流）需靠讀法補才懂**（概念圖不自明，輕）—— 「為什麼 dim_customer 單獨一個 stage」第一眼會困惑，讀 L238 第 2 點才解。可在圖旁或讀法明點「JOIN 兩邊要各自 shuffle，所以各占一個 stage、在 Stage3 碰頭」。
5. **§1.6 麻煩二的「199 + 1 = 200」用到了麻煩三才解釋的 200**（節奏，極輕）—— 順序很快接住，可不改；若要更順，麻煩二可不寫死 200 或加半句「（為什麼 200，下一塊講）」。
6. **§1.7 仍是全章密度峰值、最該瘦身**（太密/節奏，沿用第二輪）—— spill/HDFS 吞吐/JVM GC 三黑盒詞（本輪未重審 §1.7 內文，從節奏看仍在）。**若要精簡全章，第一個動 §1.7**：JVM GC 降級/砍、代價清單拆「記憶體面/系統面」。§1.6→§1.7 兩重節連著，是全章唯一想中途停下的地方。
7. **§1.9 MapReduce / DAG / Tez 三個專有名詞未展開**（術語沒先定義，輕）—— 都只當「Hive 的某個底層引擎/規劃形態」的名字用，不影響理解（DAG 還用「就是上一節那張圖」給了直覺），可接受。標一下而已。
8. **輕微重複：240 出現 4 次、200 出現 3 次**（節奏，可不改）—— 對本讀者是強化、利大於弊；若編輯要砍字，§1.8 讀法第 3 點的回指可精簡。

### 誤讀（不改或微調）

9. **§1.2「gzip 不能從中間切開」** —— 新資訊但只是舉例、不影響主軸，可接受不改。
10. **§1.8「廣播是不是自動的、我怎麼觸發」** —— §1.8 沒說，但已標「第 03 章 broadcast join」，延後合理，不改。
11. **§1.2 流程圖與正文數字重複（30GB/128MB/240）** —— 對本讀者是再確認，正面，不改。

---

## 第三輪回傳摘要（最卡的點 + 長度節奏）

**四節讀者體驗（直接回答提問）：**
- **§1.2**：讀得懂，是新增節最成功的一節。「30GB÷128MB≈240」**對量感幫助很大**，太少/太多取捨清楚。唯一缺陷＝spill 在此首次出現但定義晚到 §1.6（時序反）。
- **§1.6 三麻煩**：**不會太多太雜**，三塊各打一痛（記憶體/等最慢的人/為什麼 200）、有遞進。「199 個三秒做完、1 個肥 task 跑五分鐘其他人乾等」**畫面非常清楚**，是本節最好的一句；「為什麼是 200」**懂了**（點明「不是延續輸入、是被重設成預設 200」）。維持三塊，不砍。
- **§1.8**：**整章最有收穫的一節、不複雜**——是「把零件拼起來」的高潮。四個 stage **跟得上但要對著讀法慢讀一次**；最大卡點＝write/read 跨 stage 配對沒點明。「小表 broadcast 免一個 shuffle」**精準打到「寫法差很多」**，是我最想記住的一句。
- **§1.9**：對同時用 Hive 的我**有用且看得懂**；「job 之間落地 HDFS vs 記憶體 pipeline」對上了 §1.6 的磁碟慢直覺、DAG 用「就是上一節那張圖」回指很聰明；「別過度宣稱（你的 Hive 可能是 Tez）」很誠實、避免我誤用結論。

**最卡的點（兩個，都不算重）：**
1. **§1.2 spill 時序**——spill 先用（§1.2）後定義（§1.6），§1.6 還說「記住這個詞」像首次登場。
2. **§1.8 stage 圖 write/read 跨邊界配對沒明說**——這是「最有價值的圖」唯一沒一眼懂的點，補一句就升級。

**長度與節奏判斷：偏長，但內容該留——問題在密度不均、不在冗。**
- **移動 §1.7（executor 移到 shuffle 之後）是對的改動**，解掉了第二輪「executor 依賴還沒學的 spill」。
- **新峰值在 §1.6→§1.7**（兩重節連著），**§1.7 仍是全章最該瘦身的一節**：若要精簡，第一個動 §1.7（JVM GC 降級/砍、代價清單拆段）。
- **§1.8→§1.9→§1.10 收尾曲線很舒服**（高潮→喘息→收束），不要動。
- 10 節已到單章上限，**不該再加新概念**；輕微重複（240×4、200×3）對本讀者是強化、可不改。

