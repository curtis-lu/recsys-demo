# Spark 手冊 round-3：同事閱讀回饋修訂計畫

> 分階段執行，每階段一個 commit，做完貼 diff 給 user 確認再進下一階段。
> 方法論沿用 round-2（[`2026-06-22-spark-handbook-round2-revision-plan.md`](2026-06-22-spark-handbook-round2-revision-plan.md)）：三級 triage、審稿後加料補審、連結健檢、C 一致性複核。

**分支**：`feat/spark-tuning-handbook`（落後 main 32 commit，純文件不衝突；全部階段做完再 sync main 並驗 merge-base diff 乾淨後開 PR）。

## 回饋來源（同事閱讀後）

1. 專案限定約束洩漏進手冊（No UDF／無外網被寫成手冊自身紀律）。
2. 生硬／像機器寫的措辭：`但別過度宣稱`、滿篇 `——`、`驗屍`、`餓死同事`、`一個真的會中、一個別誇大`。
3. 引用錯誤：§3.2 說 pushdown 在「下一節」其實在 §3.4。
4. 每章想要章內目錄（快速跳到指定段落）。
5. （本輪新增）把 ch11 場景索引內容搬進 index 首頁。

## 階段

- [x] **Phase 1 — 移除專案約束洩漏**（技術正確性，最高優先）｜commit `39a2ef5`
  - ch10 §10.4 + 來源 footer：「No UDF 紀律／生產禁用」改為通用技術理由（逐列跨 JVM↔Python 序列化慢、對 Catalyst 是黑盒），治理禁令降為條件式。
  - ch09 §9.x：「違反 No UDF／無外網的紀律」改為真正架構理由（executor fan-out、無法控速、打爆 rate limit），外網限制改條件式。
  - 審核點：殘留掃描 0、diff 僅 3 處。✅

- [x] **Phase 2 — 修正交叉引用**（技術正確性）｜commit `d6b76af`
  - ch03 §3.2 L74：「下一節的 pushdown」→「§3.4 的 pushdown」。
  - ch03 §3.4 L122：「下一節會看到 join key 隱式轉換」→「後面 §3.7 會看到」。
  - 審核點：舊措辭清除、其餘 `下一節/下節` 7 處複查皆正確或已標章號。✅

- [ ] **Phase 3 —【結構】把 ch11 場景索引併入 index + ch12→ch11 重編號**
  - **動機**：首頁本就是入口／導覽，「你的工作屬哪種場景 → 讀哪幾章」放首頁最合用；ch11（`11-scenario-playbooks.md`）即此內容。
  - **步驟**：
    1. 盤點 ch11 與 index 既有「如何使用本手冊／兩條主線」的重疊，**合併去重**（非直接貼）。最終 index 結構：簡介 → 場景對應（場景→章＋頭號雷＋主線）→ 章節導覽表 → 順手修掉已知 bug（導覽表 01–08 純文字 vs 09–12 連結不一致 → 統一全連結）。
    2. `12-cheatsheet-and-glossary.md` → `11-cheatsheet-and-glossary.md`（`git mv`）；標題 `# 12 ·…`→`# 11 ·…`、內部 `§12.x`→`§11.x`。
    3. 刪除 `11-scenario-playbooks.md`。
    4. 全書交叉引用更新：grep `第 11 章|第 12 章|§11|§12|11-scenario|12-cheatsheet` → 指向舊場景章的改指 index 場景段；指向舊速查章的改成新「第 11 章」／新檔名。**單次掃描函式型 `re.sub`** 避免連鎖誤替。
    5. prev/next footer：ch10「下一章 第 11 場景」→「下一章 第 11 速查與名詞表」；新 ch11（原 12）「上一章 第 11」→「上一章 第 10」。
  - **章數 12→11**（連續、不留空號）。
  - **風險**：重編號連鎖誤替（用單次 re.sub）；index 變長需順過行文。
  - **審核點**：連結健檢（相對連結＋錨點）0 斷；C 一致性複核（章序／footer／導覽表／交叉引用）；章數＝11。

- [ ] **Phase 4 — 替換生硬詞（含標題改名連動錨點）**
  - `餓死同事`（ch01／ch04，**§4.7 在標題**）→「把資源佔光／排擠別的作業」一類。
  - `驗屍`（ch02／ch06，**§2.11 在標題**）→「完整診斷／逐項排查／拆解」一類。
  - `但別過度宣稱`（ch01 L294）→ 改成不對讀者喊話的寫法。
  - ch05 §5.4「真的會中／別誇大」→「一個確實成立、一個被誇大」。
  - ⚠️ §2.11／§4.7／§5.4 在標題，改名動到錨點 → 先 grep 出所有引用該錨點處（已知 ch02 有 2 處連 §4.7）一起改，改完跑連結健檢。
  - 審核點：逐詞 diff＋連結健檢 0 斷。

- [ ] **Phase 5 — 破折號 `——` 減量**（風格，需校準）
  - 全書約 600+（每章 41–66）。先只改 **ch01** 當樣本（→約 10–15），**給 user 看 before/after 校準口味**，點頭後才套其餘各章。
  - 主迴圈手改、逐章重讀，不外派，避免品質飄移。

- [ ] **Phase 6 — 每章加章內目錄（快速跳轉）**
  - 每章「本章前提」callout 後、§x.1 前，加 `## 本章目錄`，GitHub 錨點連結列出該章所有 `## x.y`。
  - **排最後**：用 Phase 4 改名後的最終標題＋Phase 3 重編號後的最終章號產錨點；產生後跑連結健檢。

## 收尾

sync main（按既有程序處理 graphify hook：切換／合併前 `git checkout -- graphify-out/GRAPH_REPORT.md`，逐步檢查 HEAD）→ 驗 merge-base diff 乾淨 → 依 user 決定開 PR 給同事看。
